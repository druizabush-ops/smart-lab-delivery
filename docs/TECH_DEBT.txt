Название: Реестр технического долга

[Технический долг]
Дата: 2026-04-13
Ветка: work
Блок: Контракты интеграции Renovatio
Что временно: Документация пока содержит концептуальные сущности без формальных JSON-схем и без версионированного контракта.
Почему это временно: На текущем этапе приоритет — зафиксировать архитектурную рамку и правила проекта.
Почему принято именно сейчас: Нужно начать с устойчивого каркаса до реализации адаптеров.
Какие ограничения: Высокий риск корректировок при первом подключении к реальному API.
Когда пересмотреть: Перед реализацией integration/renovatio клиента.
Что будет признаком, что пора заменить решение: Появление официальных контрактов/примеров ответов МИС.

[Технический долг]
Дата: 2026-04-13
Ветка: work
Блок: Правила ретраев и дедупликации доставки
Что временно: В статусной модели зафиксированы базовые переходы без детальной retry-политики, backoff-стратегии и критериев дедупликации.
Почему это временно: Политика зависит от SLA каналов MAX/email и операционных ограничений клиники.
Почему принято именно сейчас: Без согласованных SLA формализация ретраев может привести к ложным ограничениям.
Какие ограничения: Поведение при множественных ошибках пока не специфицировано полностью.
Когда пересмотреть: На этапе реализации application/delivery.
Что будет признаком, что пора заменить решение: Утверждение бизнес-правил и лимитов повторных отправок.

[Технический долг]
Дата: 2026-04-13
Ветка: work
Блок: Выбор хранилища и миграционной стратегии
Что временно: Не выбраны целевая СУБД, стратегия миграций и требования к аудиту неизменяемых событий.
Почему это временно: На стадии каркаса важно сначала стабилизировать модель слоев и документов.
Почему принято именно сейчас: Ранний выбор БД до фиксации доменных контрактов может привести к преждевременной оптимизации.
Какие ограничения: Нельзя оценить эксплуатационные характеристики и финальный формат персистентности.
Когда пересмотреть: Перед началом реализации infrastructure/persistence.
Что будет признаком, что пора заменить решение: Утверждение доменной модели карточки доставки и нагрузки по операциям.

[Технический долг]
Дата: 2026-04-13
Ветка: work
Блок: Формализация операторского аудита
Что временно: Требования к идентификатору оператора, обязательности комментариев и аудиту ручных действий пока описаны только на уровне намерений.
Почему это временно: Нужна совместная проработка с владельцами процесса и требованиями ИБ.
Почему принято именно сейчас: Без согласования бизнес-процесса нельзя корректно зафиксировать обязательные поля и ограничения.
Какие ограничения: Сценарии manual_review пока не готовы к реализации в коде.
Когда пересмотреть: До проектирования presentation/operator_panel и связанных application-сценариев.
Что будет признаком, что пора заменить решение: Утвержденный регламент ручной обработки исключений.

[Технический долг]
Дата: 2026-04-13
Ветка: work
Блок: Упрощенная retry/fallback оркестрация без инфраструктурного планирования
Что временно: RetryPolicy/FallbackPolicy работают синхронно и применяются только в рамках вызова use case без таймеров, backoff и очередей исполнения.
Почему это временно: Текущий блок ограничен business-правилами и не включает инфраструктурный runtime.
Почему принято именно сейчас: Нужно стабилизировать policy-слой до подключения БД/очередей и реального транспорта.
Какие ограничения: Нельзя автоматически запускать отложенные повторные попытки по времени.
Когда пересмотреть: На этапе infrastructure runtime (queue/scheduler/outbox).
Что будет признаком, что пора заменить решение: Появление фонового исполнителя и требований SLA по времени повторов.

[Технический долг]
Дата: 2026-04-13
Ветка: work
Блок: Runtime-only deduplication без персистентности
Что временно: DeduplicationPolicy проверяет дубли только по in-memory состоянию DeliveryCard и истории attempts.
Почему это временно: В текущем блоке запрещены БД и persistent репозитории.
Почему принято именно сейчас: Нужна базовая защита от повторной отправки уже доставленных карточек до инфраструктурного этапа.
Какие ограничения: Нет глобальной dedup-защиты между процессами и после рестарта сервиса.
Когда пересмотреть: При добавлении persistence-слоя карточек доставки.
Что будет признаком, что пора заменить решение: Введение устойчивого хранилища и необходимость защиты от конкурентной обработки.

[Технический долг]
Дата: 2026-04-13
Ветка: work
Блок: Временный runtime/infrastructure слой на in-memory реализациях
Что временно: DeliveryRuntime, репозиторий карточек и queue реализованы in-memory, execution loop синхронный без внешнего scheduler.
Почему это временно: Текущий этап целенаправленно строит управляемый runtime-мост к production инфраструктуре без ранней операционной сложности.
Почему принято именно сейчас: Нужна исполняемость сценариев и operator-facing state до подключения реальной БД/брокера.
Какие ограничения:
- Состояние теряется при рестарте процесса.
- Нет горизонтального масштабирования очереди.
- Нет SLA-управления по времени повторов.
Когда пересмотреть: На этапе real persistence + real transport + scheduler.
Что будет признаком, что пора заменить решение: Утверждение требований к отказоустойчивости, межпроцессной координации и долговременному хранению карточек.

[Технический долг]
Дата: 2026-04-13
Ветка: work
Блок: Ограничения runtime queue/retry и in-memory жизненного цикла
Что временно:
- Runtime обрабатывает только один управляемый синхронный цикл (`seed -> run_once`) и не выполняет автоматическую повторную постановку карточек в очередь при `waiting_retry`.
- Queue/repository модель полностью in-memory и не сохраняет устойчивое состояние между перезапусками процесса.
Почему это временно: На текущем этапе запрещено добавлять scheduler/background workers и persistent storage, а задача ограничена точечным исправлением runtime summary/id-консистентности.
Почему принято именно сейчас: Важно явно зафиксировать текущее поведение как осознанное ограничение, а не скрытую реализацию.
Какие ограничения:
- Для `waiting_retry` не происходит time-based или автоматический re-enqueue в рамках runtime.
- Повторные process cycle зависят от жизненного цикла процесса и не обеспечивают persistent dedup между запусками.
Когда пересмотреть: В следующем крупном infrastructure-блоке при внедрении persistence + scheduler/process manager.
Что будет признаком, что пора заменить решение: Появление требований к автоматическим отложенным retry, межпроцессной очереди и устойчивому состоянию карточек.


[Технический долг]
Дата: 2026-04-13
Ветка: work
Блок: Persistence слой первой итерации
Что временно:
- Репозиторные integration-тесты выполняются на SQLite in-memory контуре как быстрый surrogate вместо поднятия реального PostgreSQL в CI.
- Runtime пока не содержит scheduler/re-enqueue manager для time-based retry между process cycle.
Почему это временно: Цель блока — добавить foundation persistence без преждевременной production-сложности.
Какие ограничения:
- Нет полного e2e с живой PostgreSQL в автоматическом контуре.
- WAITING_RETRY карточки не планируются автоматически по времени.
Когда пересмотреть: При внедрении CI-сервиса с тестовым PostgreSQL и планировщика выполнения.

[Технический долг]
Дата: 2026-04-14
Ветка: work
Блок: Sync Scheduler / Process Manager без фонового планирования
Что временно:
- DeliveryProcessManager реализован в синхронном режиме и запускается вручную через CLI.
- Отсутствуют cron/background таймеры и автоматическое time-based окно запуска retry-циклов.
Почему это временно: Цель блока — управляемый lifecycle поверх текущего runtime без premature production-orchestrator.
Какие ограничения:
- Повторная обработка зависит от ручного запуска process manager цикла.
- Нет гарантированного SLA по времени между retry-итерациями.
Когда пересмотреть: При переходе к operator command-side и production runtime требованиям.
Что будет признаком, что пора заменить решение: Появление требований на автономные расписания и фоновое выполнение.

[Технический долг]
Дата: 2026-04-14
Ветка: work
Блок: Отсутствие distributed execution и broker orchestration
Что временно:
- Нет Redis/RabbitMQ/Kafka и нет distributed scheduler.
- Нет межпроцессной координации parallel execution для карточек.
Почему это временно: Текущий этап ограничен локально воспроизводимым процесс-менеджером.
Какие ограничения:
- Горизонтальное масштабирование execution пока отсутствует.
- Нет защиты от конкурентных запусков на уровне внешней оркестрации.
Когда пересмотреть: После стабилизации real transport и operator command layer.
Что будет признаком, что пора заменить решение: Потребность в multi-instance исполнении и гарантированной распределенной координации.

[Технический долг]
Дата: 2026-04-14
Ветка: work
Блок: Identity mapping patient <-> MAX recipient
Что временно: Для real MAX transport соответствие patient_id и recipient_id задается через env-карту `SLD_MAX_PATIENT_RECIPIENT_MAP`.
Почему это временно: Нет централизованного identity-link слоя и подтвержденного patient-facing контракта авторизации в MAX.
Какие ограничения: Без записи mapping отправка через MAX возвращает ошибку и не выполняется.
Когда пересмотреть: При проектировании patient identity + mini app access control.

[Технический долг]
Дата: 2026-04-14
Ветка: work
Блок: MAX mini app contract без UI-реализации
Что временно: Подготовлен только deep-link/startapp контракт без полноценного mini app интерфейса.
Почему это временно: Текущий блок ограничен transport integrations и исключает UI.
Какие ограничения: Нет пользовательского экрана результата внутри MAX, только подготовка точки входа.
Когда пересмотреть: На patient-facing mini app блоке.

[Технический долг]
Дата: 2026-04-14
Ветка: work
Блок: Минимальный SMTP провайдер
Что временно: Email adapter реализован через базовый SMTP-контур без provider-specific API, шаблонов и операционного мониторинга.
Почему это временно: Для текущего этапа нужен рабочий транспорт без enterprise hardening.
Какие ограничения: Нет delivery analytics, bounce handling и расширенной маршрутизации.
Когда пересмотреть: При переходе к production hardening email-канала.

[Технический долг]
Дата: 2026-04-14
Ветка: work
Блок: Частичное покрытие Renovatio API
Что временно: Реальный Renovatio adapter покрывает только `getPatient`, `getPatientLabResults`, `getPatientLabResultDetails`.
Почему это временно: Реализован минимально достаточный контракт для текущего delivery flow.
Какие ограничения: Расширенные сценарии (файлы/PDF, дополнительные фильтры и поля) не реализованы полностью.
Когда пересмотреть: При расширении delivery pipeline и требованиях к вложениям.

[Технический долг]
Дата: 2026-04-14
Ветка: work
Блок: Dual-mode интеграционные адаптеры в одном классе
Что временно:
- MAX/Email/Renovatio адаптеры совмещают `stub` и `real` поведение внутри одной реализации.
Почему это временно: Это упрощает текущее тестирование и wiring контейнера, но смешивает ответственность режимов.
Какие ограничения:
- Требуется явный контроль mode на уровне окружения и тестов.
- Повышается риск регрессий между stub- и real-ветками поведения.
Когда пересмотреть: При следующем этапе production hardening real integrations.
Что будет признаком, что пора заменить решение: Появление отдельных реализаций/интерфейсных биндингов для stub и real адаптеров.

[Operator command layer]
- Audit trail реализован в минимальном варианте (инфраструктурный stdout-лог через OperatorActionLoggerAdapter), без отдельной таблицы operator_action_log.
- Полноценная RBAC/ACL модель для operator command endpoint-ов не внедрена, доступ предполагается доверенным контуром.
- Distributed locking/concurrency control для одновременных operator commands не реализован сознательно на текущем этапе.
- Набор команд ограничен retry/manual-review/requeue/override-channel; расширение команд требует отдельного архитектурного решения.

[Технический долг]
Дата: 2026-04-14
Ветка: work
Блок: Operator workflow model пока ограничен command-layer policy
Что временно:
- Requeue в operator layer реализован как controlled preparation (FAILED -> NOT_STARTED -> ACTIVE) внутри command use case без выделенного сценарного workflow-сервиса.
- Override channel разрешен только в безопасных состояниях (не terminal delivery, не queue DONE, не same-channel), но без расширенной контекстной валидации (роль оператора, причино-следственные ограничения, lock-конкуренция).
- Retry command остается thin-wrapper над existing RetryDeliveryUseCase без отдельного operator retry pipeline.
Почему это временно: Текущий блок целенаправленно ограничен точечным hardening operator command layer без расширения архитектуры domain/application.
Какие ограничения:
- Нет полноценной state-machine модели operator workflow с отдельными сценарными переходами.
- Нет формального rule-trace/audit-механизма объяснения политик кроме reason-строк и action logger.
Когда пересмотреть: На этапе production hardening operator workflow и security/rbac.
Что будет признаком, что пора заменить решение: Требование многошаговых operator сценариев, конкурентного контроля и расширенного governance-а ручных действий.

[TD-0010] MAX UI package временно заменен shim-слоем
Дата: 2026-04-14
Описание: `@maxhub/max-ui` недоступен из текущего окружения (403 private registry). Используется локальный alias/shim c совместимыми компонентами-обертками.
Риск: Возможны расхождения API/стилей после подключения официального SDK.
План закрытия: Подключить реальный пакет из разрешенного registry и прогнать UI regression smoke.

[TD-0011] initData/start_param не проходят серверную криптовалидацию
Дата: 2026-04-14
Описание: Mini app читает `window.WebApp.initDataUnsafe` и `start_param`, но не выполняет серверную верификацию подписи.
Риск: Нельзя доверять user/patient context на клиенте в production.
План закрытия: Ввести backend endpoint валидации initData + signer-check и выдачу short-lived patient token.

[TD-0012] Document URL в patient API пока stub/readiness
Дата: 2026-04-14
Описание: Patient API возвращает readiness + URL контракта `/patient/results/{id}/document`, реальная выдача файла не реализована.
Риск: Отсутствие финального UX скачивания результатов.
План закрытия: Реализовать secure file streaming endpoint с audit и access-check.

[Технический долг]
Дата: 2026-04-14
Ветка: work
Блок: MAX UI dependency в mini app frontend
Что временно:
- В `frontend/miniapp` добавлена зависимость `@maxhub/max-ui` c версией `latest`, но фактический импорт UI-компонентов по-прежнему идет через локальный shim (`src/max/maxUiShim.tsx`) через alias в Vite/Vitest.
Почему это временно: Нужна сборка и тестируемость mini app до подтверждения стабильного production-контракта внешнего UI SDK.
Какие ограничения:
- Даже при установленном пакете поведение UI контролируется shim-адаптером.
- Возможны расхождения визуального/поведенческого контракта относительно реального MAX UI.
Когда пересмотреть: При интеграции реального MAX UI набора компонентов в patient-facing mini app.
Что будет признаком, что пора заменить решение: Подтвержденный контракт и стабильная версия `@maxhub/max-ui` для прямого использования без shim.

[Технический долг]
Дата: 2026-04-14
Ветка: work
Блок: Frontend test infrastructure stabilization
Что временно:
- Тестовый контур mini app зафиксирован на `vitest + jsdom + globals` и опирается на глобальные моки (`globalThis.fetch`) для API сценариев.
Почему это временно: Текущая задача ограничена стабилизацией сборки/тестов без расширения frontend-архитектуры.
Какие ограничения:
- Нет изолированного MSW/contract-теста с реальными сетевыми сценариями.
- Часть интеграционных UI-потоков внутри MAX runtime пока покрывается только unit-тестами.
Когда пересмотреть: На этапе расширения patient-facing UI и e2e контура mini app.
Что будет признаком, что пора заменить решение: Появление обязательных e2e/contract требований для frontend слоя.


[TD-0012] Частичная модель security в patient контуре (relaxed fallback)
Описание: В `SLD_PATIENT_SECURITY_MODE=relaxed` сохранен dev/test fallback на query `patient_id` без обязательного initData.
Риск: Ошибочная эксплуатация relaxed-режима в prod может ослабить защиту.
План закрытия: Сделать strict-режим дефолтным для prod-like и добавить выдачу short-lived server token после валидации initData.

[TD-0013] Rate limiting реализован in-memory middleware
Описание: Лимитирование частоты сейчас локальное и не синхронизируется между инстансами.
Риск: При масштабировании API ограничение может работать непоследовательно.
План закрытия: Вынести лимитирование в централизованный backend (gateway/redis).

[TD-0014] Observability без внешней платформы
Описание: Structured logging и health/readiness внедрены, но нет централизованного log/metric/tracing backend.
Риск: Ограниченная глубина расследований в multi-node среде.
План закрытия: Подключить внешнюю платформу observability после стабилизации runtime нагрузочного профиля.

[Launch blockers]
Дата: 2026-04-14
- До prod-like запуска должны быть заданы все критичные env/секреты; иначе launch validator блокирует старт.
- Требуется подтвержденный рабочий канал MAX recipient mapping для целевой patient базы.

[Post-launch improvements]
Дата: 2026-04-14
- Перевести rate limiting/audit buffering на внешние распределенные компоненты.
- Расширить recovery-план до инфраструктурного DR уровня (вне текущего блока).

[Технический долг]
Дата: 2026-04-16
Ветка: work
Блок: Временные диагностические признаки mini app UI
Что временно:
- В App.tsx добавлено dev-only отображение `insideMax`, `hasInitData`, `patientId` для диагностики staging-инцидентов.
Почему это временно: Поля нужны для оперативной верификации MAX контекста в тестовом контуре.
Какие ограничения: При отключенном dev-mode признаки не видны; для prod-диагностики нужен отдельный observability канал.
Когда пересмотреть: После стабилизации MAX mini app e2e и закрытия staging-регрессий.

[Технический долг]
Дата: 2026-04-16
Ветка: work
Блок: Ограничения MAX context без серверной валидации
Что временно:
- Клиент читает `initData`/`initDataUnsafe`, но доверие к контексту остается ограниченным без backend signer-check.
Почему это временно: Цель текущего блока — консолидация staging-фиксов без расширения security-архитектуры.
Какие ограничения: `user.id` и `start_param` нельзя использовать как единственный доверенный источник идентичности.
Когда пересмотреть: При внедрении полноценной серверной валидации WebAppData и short-lived access token.

[Технический долг]
Дата: 2026-04-16
Ветка: codex/consolidate-staging-fixes-into-codebase
Блок: Диагностический Renovatio real script не заменяет patient auth flow
Что временно:
- `scripts/test_renovatio_real.py` добавлен как локальная проверка живого HTTP-контракта и auth Renovatio.
- В диагностике используется `patient_id=1` как техническая точка вызова.
Почему это временно: Полноценные сценарии patient identification/authPatient/checkAuthCode еще не реализованы в продуктовой цепочке.
Какие ограничения:
- Успех диагностического script не означает готовность user-facing onboarding flow.
- Отсутствие `patient_id=1` в целевой системе не является дефектом контракта само по себе.
Когда пересмотреть: При реализации полноценного patient identification/auth flow и связывания MAX ↔ patient.
Что будет признаком, что пора заменить решение: Появление production-safe сценария идентификации пациента и авторизации без ручного ввода диагностических идентификаторов.

[Технический долг]
Дата: 2026-04-16
Ветка: work
Блок: Renovatio patient auth flow пока диагностический
Что временно:
- Patient-facing auth методы (`authPatient/checkAuthCode/refreshPatientKey/getPatientInfo/getPatientLabResults/getPatientLabResultDetails`) добавлены в интеграционный адаптер как foundation, но используются только диагностическим script.
- Runtime lifecycle `patient_key` не встроен в production backend flow.
- Production session, identity binding и MAX ↔ patient linking не реализованы.
Почему это временно: Текущий блок ограничен подготовкой корректного контракта и локальной проверкой без изменения patient API/mini app.
Какие ограничения:
- Нет серверного хранения/обновления patient_key.
- Нет полноценного production login flow пациента.
Когда пересмотреть: На следующем этапе интеграции patient-facing backend контура.
Что будет признаком, что пора заменить решение: Переход к production patient auth/session management и secure identity-linking между MAX и пациентом.

Patient Auth ограничения / техдолг:
- refresh lifetime зависит от ограничений Renovatio API и может отличаться от локального TTL;
- текущая phone-ветка не хранит промежуточный challenge server-side (требуется усиление anti-replay);
- in-memory session repository нужно заменить на persistent store для multi-instance production.

[Технический долг]
Дата: 2026-04-21
Ветка: work
Блок: Зависимость patient login flow от getPatientInfo
Что временно: Создание patient session после `authPatient` требует обязательного шага `getPatientInfo(patient_key)`; при частичном сбое (`auth ok`, `profile fail`) login завершается controlled ошибкой.
Почему это временно: Внешний API Renovatio разделяет auth и профиль, но не дает транзакционной гарантии между вызовами.
Какие ограничения:
- Доступность `getPatientInfo` напрямую влияет на успешность login.
- Возможны partial failure сценарии: ключ получен, но профиль временно недоступен.
Когда пересмотреть: При появлении в Renovatio объединенного/атомарного patient auth+profile контракта или при внедрении устойчивой retry/circuit-breaker политики для patient auth flow.
Что будет признаком, что пора заменить решение: Рост ошибок profile fetch в проде или подтвержденный контракт, возвращающий профиль в auth-ответе.

[Технический долг]
Дата: 2026-04-21
Ветка: work
Блок: Вариативность URL-контрактов Renovatio между инсталляциями
Что временно:
- В integration adapter зафиксирована allowlist-стратегия: часть patient-facing методов (`authPatient`, `checkAuthCode`, `refreshPatientKey`, `getPatientInfo`, `getPatientLabResults`, `getPatientLabResultDetails`) всегда вызывается по unversioned пути.
Почему это временно: Контракт Renovatio неоднороден между установками (v1/v2/unversioned), а единый discovery-механизм методов со стороны API отсутствует.
Какие ограничения:
- При подключении новых patient-facing методов требуется ручная валидация route strategy и обновление allowlist.
- Возможны отличия поведения между staging/prod окружениями одной интеграции.
Когда пересмотреть: При появлении официального capability/discovery механизма Renovatio или унифицированного versioned контракта во всех целевых инсталляциях.
Что будет признаком, что пора заменить решение: Появление documented API matrix с гарантированной поддержкой versioning по всем patient-facing методам.
