Название проекта: Smart Lab Delivery

1. Цель проекта
Создать устойчивую систему автоматической доставки лабораторных результатов пациентам с минимальным ручным участием и прозрачной операторской обработкой исключений.

2. Бизнес-суть проекта
- Источник данных: МИС Renovatio.
- Основной канал отправки: MAX.
- Резервный канал отправки: email.
- Система должна формировать внутреннюю очередь карточек доставки, хранить статусы, историю попыток и действий оператора.

3. Основные правила проекта
- Явность важнее краткости.
- Все значимые изменения фиксируются в журнале.
- Комментарии в коде и документация ведутся на русском языке.
- Статусы централизуются в доменном слое.
- Временные решения обязательно маркируются как технический долг.

4. Архитектурные принципы
- МИС Renovatio — источник истины.
- Внутренний сервис — оркестратор процесса доставки.
- Разделение на слои: integration / application / domain / infrastructure / presentation.
- Бизнес-логика не смешивается с транспортом и UI.

5. Границы проекта
В рамках текущего этапа создан архитектурный каркас, документационная база и правила сопровождения. Сложная бизнес-логика сознательно отложена до согласования контрактов и сценариев.

6. Основные сущности системы
- Пациент
- Лабораторный результат
- Карточка доставки
- Попытка отправки
- Ошибка доставки
- Операторский комментарий

7. Статусы и правила изменения статусов
Актуальная модель статусов и переходов ведется в отдельном документе: docs/STATUS_RULES.txt.

8. Основные интеграции
- МИС Renovatio (получение данных и документов)
- MAX (основная доставка)
- Email SMTP/API (резервная доставка)

9. Источники истины и потоки данных
- Данные пациента и результатов — из Renovatio.
- Внутренний реестр карточек — источник операционного состояния доставки.
- История отправок и ошибок — внутренний неизменяемый журнал событий.

10. Структура проекта по папкам
- docs/
- src/integration/renovatio/
- src/application/{queue,delivery,orchestration}/
- src/domain/{entities,statuses,contracts}/
- src/infrastructure/{max,email,persistence,logging}/
- src/presentation/operator_panel/
- tests/{unit,integration,smoke}/
- scripts/
- config/

11. Соглашения по коду
- Магические значения запрещены.
- Контракты и статусы описываются централизованно.
- Один модуль — одна ответственность.
- Неявные допущения запрещены: фиксируются в документации.

12. Соглашения по комментариям
- Комментарии на русском языке.
- Для важных файлов обязателен заголовок с назначением и ограничениями.
- Для сложных ветвлений и интеграционных точек обязательны пояснения причин.

13. Соглашения по журналированию изменений
- Каждое существенное изменение фиксируется отдельной записью.
- Форматы записей: [Изменение], [Багфикс], [Решение], [Технический долг].
- Записи вносятся в этот файл в хронологическом порядке без удаления предыдущих записей.

14. Реестр принятых решений
Поддерживается в docs/DECISIONS.txt.

15. История изменений по блокам

[Изменение]
Дата: 2026-04-13
Ветка: work
Блок: Инициализация инженерного каркаса
Файлы: README.md; docs/PROJECT_JOURNAL.txt; docs/ARCHITECTURE_OVERVIEW.txt; docs/STATUS_RULES.txt; docs/SETUP_WINDOWS.txt; docs/DECISIONS.txt; docs/TECH_DEBT.txt; структура каталогов src/, tests/, scripts/, config/
Причина: Зафиксировать архитектурную основу проекта до начала реализации бизнес-логики.
Что сделано: Создан каркас каталогов и стартовый комплект обязательной документации.
Что изменилось в логике: Бизнес-логика не добавлялась.
Что изменилось в архитектуре: Закреплена целевая слоистая архитектура и зона ответственности каждого слоя.
Что нужно проверить: Полноту структуры, согласованность терминов, готовность к следующему этапу (контракты Renovatio и модель карточки).
Риски / ограничения: Контракты внешних API пока не формализованы; требуется последующая детализация схем данных.
Связанные блоки: Архитектура, статусная модель, журнал решений, технический долг.

[Изменение]
Дата: 2026-04-13
Ветка: work
Блок: Ревизия и усиление стартового инженерного каркаса
Файлы: README.md; docs/ARCHITECTURE_OVERVIEW.txt; docs/PROJECT_JOURNAL.txt; docs/STATUS_RULES.txt; docs/DECISIONS.txt; docs/TECH_DEBT.txt; docs/SETUP_WINDOWS.txt
Причина: Проверить согласованность инженерной базы и довести обязательную документацию до состояния готовности к следующему этапу.
Что сделано: Проведена ревизия структуры слоев и документов, устранены неоднозначности формулировок, усилены правила централизации статусов и фиксации допущений.
Что изменилось в логике: Бизнес-логика, интеграционные клиенты, БД, UI и доставка не реализовывались и не изменялись.
Что изменилось в архитектуре: Архитектурная рамка не менялась; дополнительно формализованы инварианты «Renovatio — источник истины» и «MAX — основной канал, email — резервный».
Что усилено:
- README уточнен как входная точка с текущей стадией и обязательными документами.
- ARCHITECTURE_OVERVIEW дополнен открытыми архитектурными вопросами и явными инвариантами.
- STATUS_RULES расширен назначением статусов, правилами изменений и запретом распыления строковых статусов.
- DECISIONS дополнен стартовыми ADR по источнику истины и приоритету каналов доставки.
- TECH_DEBT расширен реальными временными допущениями текущего этапа.
- SETUP_WINDOWS усилен проверками под Windows 10 / PowerShell.
Ограничения, которые остались:
- Нет утвержденных версионированных контрактов Renovatio.
- Не утверждены SLA/политики fallback и retry для MAX/email.
- Не выбран стек персистентности и миграционная стратегия.
- Не формализован регламент операторского аудита.
Следующий шаг разработки: Формализовать доменные контракты (пациент, результат, карточка доставки) и подготовить проект интерфейса integration/renovatio без реализации транспортных клиентов.
Связанные блоки: Архитектура, ADR, статусная модель, технический долг, правила сопровождения.

[Багфикс]
Дата: 2026-04-20
Ветка: work
Блок: Выравнивание hardening tests после перехода на patient auth/session flow
Файлы: tests/integration/test_hardening_endpoints.py; docs/PROJECT_JOURNAL.txt; docs/TEST_PLAN.txt
Причина: Интеграционный hardening тест использовал устаревший контракт (strict-проверка через operator app и старый test container без patient auth use cases), из-за чего проверки расходились с текущей patient auth/session архитектурой.
Что сделано:
- Тестовый контейнер для hardening приведен к актуальному контракту `create_patient_api_app()` и дополнен patient auth/session use cases.
- Strict-mode сценарий перенесен на корректный путь проверки через patient API (`/patient/results`) без активной сессии.
- Зафиксирована актуальная граница security: доступ к patient results разрешается только через server-side session, а не через произвольный `patient_id` из клиента.
Что изменилось в логике: Изменений production-логики нет; обновлен только интеграционный тестовый слой и документация.
Что изменилось в архитектуре: Архитектура не менялась; тесты синхронизированы с уже внедренным разделением operator/patient и session-based patient access.
Что нужно проверить: `tests/integration/test_hardening_endpoints.py`, `tests/integration/test_patient_auth_api.py` и полный `pytest` должны проходить без ослабления security-инвариантов.

16. Реестр технического долга
Поддерживается в docs/TECH_DEBT.txt.

17. Реестр рисков и ограничений
- Не утверждены финальные контракты Renovatio API.
- Не определены SLA по каналам MAX и email.
- Не определены правила ретраев и дедупликации отправок.

18. Открытые вопросы
- Какая целевая СУБД и стратегия миграций?
- Нужен ли outbox-паттерн на первом релизе?
- Как формализуется идентификатор оператора и аудит ручных действий?
- Какие ограничения по размеру/формату PDF от МИС?

19. Следующие шаги
1) Зафиксировать доменные контракты пациента, результата и карточки доставки.
2) Спроектировать централизованную status model в коде.
3) Описать контракт интеграционного клиента Renovatio.
4) Подготовить минимальный тест-план для критичных сценариев доставки.

20. Примечание по непрерывности журнала
Этот файл является главным непрерывным журналом проекта и должен обновляться при каждом существенном изменении.

[Изменение]
Дата: 2026-04-13
Ветка: work
Блок: Проектирование доменной модели
Файлы: src/domain/__init__.py; src/domain/entities/__init__.py; src/domain/entities/channels.py; src/domain/entities/patient.py; src/domain/entities/lab_result.py; src/domain/entities/delivery_card.py; src/domain/entities/delivery_attempt.py; src/domain/statuses/__init__.py; src/domain/statuses/lab_result.py; src/domain/statuses/delivery.py; src/domain/statuses/attempt.py; src/domain/statuses/queue.py; docs/PROJECT_JOURNAL.txt
Причина: Перейти от инженерного каркаса к первому функциональному слою и зафиксировать доменные инварианты до реализации application/integration.
Что сделано:
- Реализованы сущности Patient, LabResult, DeliveryCard, DeliveryAttempt как изолированные доменные модели.
- Добавлен централизованный модуль src/domain/statuses с enum для групп lab_result, delivery, attempt, queue.
- Для DeliveryCard введены контролируемые методы create(), add_attempt(), change_status() и запрещены невалидные переходы.
- Для DeliveryAttempt введены инварианты согласованности result и error_message.
Какие решения приняты:
- Словарь статусов хранится только в enum доменного слоя.
- История попыток в DeliveryCard трактуется как append-only список.
- Прямое изменение статуса вне доменных методов считается нарушением модели.
Ограничения:
- Не реализованы интеграции Renovatio/MAX/email, БД, очереди, UI и любые инфраструктурные адаптеры.
- Не реализована политика retry/лимитов попыток (будет в application-слое).
Допущения:
- Patient и LabResult реализованы как неизменяемые dataclass, DeliveryCard — как изменяемый агрегат с инкапсулированными методами состояния.
- Временные метки создаются на UTC (datetime.utcnow) до выбора общей time policy проекта.
Что не реализовано сознательно:
- Оркестрация сценариев доставки.
- Транспортные клиенты и технические статусы инфраструктуры.
- Механизмы сохранения и аудита в хранилище.
Open questions / Tech debt:
- Нужно подтвердить максимально допустимое число попыток доставки и финальные условия перехода в exhausted.
- Нужно согласовать единую time policy (aware datetime, часовые пояса) для всех слоев.
Следующий шаг: Спроектировать application-слой оркестрации сценариев на основе текущих доменных сущностей и формализовать политики retry/fallback без привязки к конкретной инфраструктуре.

[Багфикс]
Дата: 2026-04-13
Ветка: work
Блок: Точечная доработка согласованности domain-статусов
Файлы: src/domain/entities/delivery_card.py; src/domain/entities/delivery_attempt.py; src/domain/entities/channels.py; src/domain/statuses/attempt.py; docs/STATUS_RULES.txt; docs/PROJECT_JOURNAL.txt
Причина: В доменном ядре были рассогласования: queue_status существовал отдельно от центральной сущности DeliveryCard, а AttemptStatus смешивал этапы жизненного цикла с терминальными исходами при поле result.
Что сделано:
- QueueStatus встроен в DeliveryCard как queue_status с инициализацией через create() и контролируемым методом change_queue_status().
- Добавлена доменная синхронизация delivery_status -> queue_status и инвариант: терминальные delivery-статусы допускаются только с queue_status=DONE.
- AttemptStatus сокращен до терминальных исходов SUCCESS/ERROR, чтобы соответствовать полю DeliveryAttempt.result.
- DeliveryChannel явно зафиксирован как доменное перечисление (value object), а не сущность.
- STATUS_RULES приведен к терминальной трактовке attempt_status.
Ограничения:
- queue_status пока описывает только доменные переходы карточки и не связан с runtime-очередями исполнения.
- Политики лимитов retry, эскалации в manual review и условия exhausted по числу попыток остаются вне domain и требуют формализации на следующем шаге.
Допущения:
- Автоматическая синхронизация queue_status из delivery_status не перезаписывает MANUAL_REVIEW при возврате в NOT_STARTED.
- DeliveryAttempt фиксирует только завершенные попытки (без промежуточных технических этапов).
Следующий шаг: Формализовать прикладные политики retry/fallback и правила перехода в EXHAUSTED/MANUAL_REVIEW в application-слое без добавления инфраструктурных адаптеров.

[Изменение]
Дата: 2026-04-13
Ветка: work
Блок: Реализация application слоя (оркестрация)
Файлы: src/application/__init__.py; src/application/interfaces/__init__.py; src/application/interfaces/delivery_provider.py; src/application/interfaces/lab_result_provider.py; src/application/interfaces/notification_logger.py; src/application/use_cases/__init__.py; src/application/use_cases/create_delivery_card.py; src/application/use_cases/process_delivery.py; src/application/use_cases/register_delivery_result.py; src/application/services/__init__.py; src/application/services/delivery_orchestrator.py; docs/PROJECT_JOURNAL.txt
Причина: Перейти к use-case оркестрации после завершения доменного слоя и зафиксировать контракты взаимодействия без инфраструктурных деталей.
Что сделано:
- Добавлены use cases: CreateDeliveryCardUseCase, ProcessDeliveryUseCase, RegisterDeliveryResultUseCase.
- Введены application-контракты: DeliveryProvider, LabResultProvider и опциональный NotificationLogger.
- Реализован сервис DeliveryOrchestrator для потока LabResult -> DeliveryCard -> Attempt -> обновление статусов.
Как устроена orchestration логика:
- DeliveryOrchestrator принимает входные доменные данные и вызывает use cases последовательно.
- CreateDeliveryCardUseCase создает карточку через доменную фабрику DeliveryCard.create() и не дублирует стартовые статусы.
- ProcessDeliveryUseCase проверяет допустимость отправки, инициирует send() через DeliveryProvider и делегирует фиксацию результата в RegisterDeliveryResultUseCase.
- RegisterDeliveryResultUseCase добавляет DeliveryAttempt через card.add_attempt(), после чего статусы обновляются исключительно доменными методами.
Что сознательно НЕ реализовано:
- HTTP/API клиенты, интеграция с Renovatio/MAX/email, БД/репозитории, очереди, retry, scheduler.
Ограничения этапа:
- Application-слой зависит только от абстракций и не содержит адаптеров инфраструктуры.
- Политики SLA/лимитов попыток и продвинутая маршрутизация каналов не добавлялись.
Следующий шаг: Реализовать инфраструктурные адаптеры к application-интерфейсам и добавить unit-тесты use-case сценариев.

[Изменение]
Дата: 2026-04-13
Ветка: work
Блок: Уточнение границ domain и application
Файлы: src/domain/entities/delivery_card.py; src/application/use_cases/process_delivery.py; src/application/use_cases/create_delivery_card.py; docs/PROJECT_JOURNAL.txt
Причина: В application-слое была размещена часть бизнес-логики, которая относится к доменным инвариантам и правилам допустимости отправки.
Какая логика была ошибочно в application:
- В ProcessDeliveryUseCase находилось правило can_be_sent по статусам delivery и queue.
- В CreateDeliveryCardUseCase дублировались доменные проверки: LabResult.status == READY и совпадение patient_id.
Как логика перенесена в domain:
- В DeliveryCard добавлен доменный метод can_be_sent(), инкапсулирующий проверку допустимости отправки по статусам.
- В DeliveryCard.create() перенесены и централизованы инварианты создания карточки: допустимость статуса результата и согласованность patient/result.
- Application use cases переведены на делегирование: ProcessDeliveryUseCase вызывает card.can_be_sent(), CreateDeliveryCardUseCase вызывает доменную фабрику без бизнес-валидации.
Какие принципы зафиксированы:
- Domain владеет бизнес-правилами и инвариантами жизненного цикла сущностей.
- Application координирует сценарий и вызывает доменные методы, не дублируя правила.
- Инварианты должны быть централизованы в доменных фабриках/методах, чтобы избежать рассинхронизации.

[Изменение]
Дата: 2026-04-13
Ветка: work
Блок: Реализация integration слоя
Файлы: src/integration/__init__.py; src/integration/renovatio/__init__.py; src/integration/renovatio/client.py; src/integration/delivery/__init__.py; src/integration/delivery/max_provider.py; src/integration/delivery/email_provider.py; src/integration/logging/__init__.py; src/integration/logging/logger_adapter.py; src/config/__init__.py; src/config/container.py; docs/PROJECT_JOURNAL.txt
Причина: После стабилизации domain/application необходимо подключить адаптеры внешнего мира через интерфейсы application и сохранить чистые границы слоев.
Какие адаптеры реализованы:
- RenovatioClient (integration/renovatio) как реализация LabResultProvider с in-memory заглушкой.
- MaxDeliveryProvider и EmailDeliveryProvider (integration/delivery) как реализации DeliveryProvider.
- LoggerAdapter (integration/logging) как реализация NotificationLogger.
Какие интерфейсы покрыты:
- LabResultProvider.get_ready_results() -> RenovatioClient.get_ready_results().
- DeliveryProvider.send(card) -> MaxDeliveryProvider.send(card) и EmailDeliveryProvider.send(card).
- NotificationLogger.log_attempt(card, attempt) -> LoggerAdapter.log_attempt(card, attempt).
Какие заглушки используются:
- RenovatioClient возвращает seed-список LabResult и фильтрует только READY.
- Delivery providers эмулируют SUCCESS/ERROR через простую deterministic-логику без внешних вызовов.
- LoggerAdapter пишет события отправки в stdout (print), без внешней системы логирования.
Dependency wiring:
- Добавлен src/config/container.py с ручной сборкой зависимостей без DI-фреймворков.
- В контейнере создаются экземпляры адаптеров, use cases и DeliveryOrchestrator.
Ограничения этапа:
- Нет реального HTTP к Renovatio.
- Нет реальной отправки в MAX/email.
- Нет БД, retry, async, очередей и production-конфигурации.
Риски интеграции:
- Renovatio: риск расхождения контрактов полей/статусов при подключении реального API.
- MAX: риск различий протокола доставки и кодов ошибок относительно stub-логики.
- Email: риск несоответствия SMTP/API ограничений, rate limits и политики отказов.
Следующий шаг: добавить unit-тесты integration-адаптеров и расширить container до выбора канала (MAX/EMAIL) в runtime без нарушения границ application/domain.

[Багфикс]
Дата: 2026-04-13
Ветка: work
Блок: Точечная доработка delivery logic перед блоком тестирования
Файлы: src/application/services/policies/delivery_policy.py; src/application/use_cases/handle_delivery_failure.py; src/application/use_cases/process_delivery.py; src/application/use_cases/retry_delivery.py; src/config/container.py; docs/STATUS_RULES.txt; docs/DECISIONS.txt; docs/PROJECT_JOURNAL.txt
Причина: Выявлены архитектурные расхождения: fallback-логика вычислялась в двух местах (DeliveryPolicy и HandleDeliveryFailureUseCase), а решение о следующем канале оставалось неявным.
Какие расхождения были найдены:
- Дублирование fallback-решений между policy-слоем и use case обработки failure.
- DeliveryDecision не нес явного результата о смене канала, из-за чего use cases могли повторно вычислять маршрут.
- Retry use case содержал собственную ветку retry-policy, что размывало границу «policy решает — use case исполняет».
Что исправлено:
- DeliveryDecision расширен полем next_channel, через которое policy явно сообщает переключение канала.
- DeliveryPolicy закреплена как единая точка business-решения после failed (retry/fallback/manual_review/exhausted).
- HandleDeliveryFailureUseCase перестал вызывать FallbackPolicy напрямую и теперь исполняет только DeliveryPolicy decision.
- ProcessDeliveryUseCase и RetryDeliveryUseCase переведены на исполнение channel-решения из DeliveryDecision без локального повторного fallback-расчета.
- Retry-модель «новый цикл через возврат в NOT_STARTED» сохранена и дополнительно зафиксирована в STATUS_RULES.
Почему DeliveryPolicy стала единой точкой решения:
- Устранены два источника истины для fallback.
- Снижена вероятность рассинхронизации правил при дальнейшем изменении лимитов/маршрутизации.
- Поведение retry/fallback стало наблюдаемым через единый decision-контракт.
Что осталось неизменным:
- Границы слоев domain/application/integration сохранены.
- Реальные HTTP/БД/очереди/async не добавлялись.
- Базовая retry-reset модель не менялась (только формализована и синхронизирована в документации).
Почему после этого можно переходить к тестированию:
- Архитектурное расхождение в delivery logic устранено до начала тестового блока.
- Правила policy теперь единообразно исполняются use cases без скрытых обходов.
- Документация приведена к фактической реализации и готова как нормативная база для тест-кейсов.

[Изменение]
Дата: 2026-04-13
Ветка: work
Блок: Реализация delivery logic (policy-слой + retry/failure use cases)
Файлы: src/application/services/policies/__init__.py; src/application/services/policies/delivery_policy.py; src/application/services/policies/retry_policy.py; src/application/services/policies/fallback_policy.py; src/application/services/policies/deduplication_policy.py; src/application/use_cases/handle_delivery_failure.py; src/application/use_cases/retry_delivery.py; src/application/use_cases/process_delivery.py; src/application/use_cases/__init__.py; src/application/services/delivery_orchestrator.py; src/application/services/__init__.py; src/config/container.py; docs/STATUS_RULES.txt; docs/PROJECT_JOURNAL.txt; docs/DECISIONS.txt; docs/TECH_DEBT.txt
Причина: Требовалось вынести business delivery logic в управляемый набор правил без внедрения инфраструктуры (БД, очередей, scheduler, реального транспорта).
Что сделано:
- В application добавлен policy-слой: DeliveryPolicy, RetryPolicy, FallbackPolicy, DeduplicationPolicy.
- RetryPolicy формализует лимиты попыток (total/MAX/EMAIL) и фиксирует трактовку failed как временной ошибки.
- FallbackPolicy реализует контролируемый переход MAX -> EMAIL без хаотичного переключения; после ошибки EMAIL карточка эскалируется в manual_review.
- DeduplicationPolicy блокирует повторную отправку успешных и exhausted карточек.
- Добавлены use cases HandleDeliveryFailureUseCase и RetryDeliveryUseCase.
- ProcessDeliveryUseCase доработан: после error запускается policy-driven failure handling вместо «немого» оставления failed.
- DeliveryOrchestrator расширен методом orchestrate_retry без раздувания в god-object.
- Container обновлен: wiring двух каналов (MAX/EMAIL), policy-объектов и новых use cases.
Как трактуются ключевые статусы:
- failed + waiting_retry: временная неудача, повтор допустим по RetryPolicy.
- exhausted: финальное автоматическое завершение, queue_status=done.
- manual_review: ручная эскалация при запрете fallback/повторов.
Ограничения:
- Нет scheduler/backoff и нет persistent deduplication.
- Retry и fallback исполняются как синхронные решения внутри use cases.
Следующий шаг: Подключить инфраструктурный runtime (очереди/планирование) и подготовить persistent модель карточек с межпроцессной дедупликацией.

[Изменение]
Дата: 2026-04-13
Ветка: work
Блок: Реализация тестового контура
Файлы: pytest.ini; tests/unit/domain/test_patient.py; tests/unit/domain/test_lab_result.py; tests/unit/domain/test_delivery_attempt.py; tests/unit/domain/test_delivery_card.py; tests/unit/application/test_policies.py; tests/unit/application/test_use_cases.py; tests/integration/test_stub_adapters_and_orchestrator.py; docs/TEST_PLAN.txt; docs/SETUP_WINDOWS.txt; docs/PROJECT_JOURNAL.txt
Причина: Зафиксировать текущее поведение domain/application/integration до следующего инфраструктурного этапа и снизить риск регрессий в delivery logic.
Какие уровни тестов добавлены:
- Unit-тесты domain слоя.
- Unit-тесты application policy/use-case слоя.
- Integration-тесты на существующих stub-адаптерах и AppContainer/DeliveryOrchestrator.
Что покрыто unit-тестами:
- Domain: инварианты Patient/LabResult/DeliveryAttempt/DeliveryCard, статусные переходы и queue-инварианты.
- Policies: RetryPolicy, FallbackPolicy, DeduplicationPolicy, DeliveryPolicy (retry/fallback/manual_review/exhausted/dedup).
- Use cases: CreateDeliveryCardUseCase, RegisterDeliveryResultUseCase, ProcessDeliveryUseCase, HandleDeliveryFailureUseCase, RetryDeliveryUseCase.
Что покрыто integration-тестами:
- RenovatioClient фильтрует только READY результаты.
- MaxDeliveryProvider/EmailDeliveryProvider воспроизводят deterministic success/error сценарии.
- AppContainer собирает контур зависимостей, DeliveryOrchestrator выполняет сквозной сценарий и retry/failure pipeline.
Критичные сценарии:
- Запрет невалидных доменных состояний и переходов.
- Согласованность retry/fallback/deduplication решений.
- Выполнение policy decision в use cases без локального дублирования логики.
- Сквозная обработка ready-результатов и retry через stub-адаптеры.
Пробелы покрытия, которые остаются:
- Нет тестов реальных HTTP/SMTP/MAX интеграций.
- Нет тестов персистентности, runtime-очередей, конкурентных сценариев и UI.
- Нет нагрузочных/soak/chaos тестов.
Минимальные изменения ради тестопригодности:
- Добавлен только `pytest.ini` для стабильного локального запуска тестов.
Новой бизнес-логики не добавлялось.
Что следующим шагом можно делать безопасно благодаря тестам:
- Переходить к инфраструктурному этапу (реальные интеграции, репозитории, runtime-механизмы) с быстрым регрессионным контролем текущего поведения delivery logic.

[Изменение]
Дата: 2026-04-13
Ветка: work
Блок: Реализация runtime / infrastructure слоя
Файлы: src/infrastructure/runtime/delivery_runtime.py; src/infrastructure/queue/in_memory_delivery_queue.py; src/infrastructure/repositories/in_memory_delivery_card_repository.py; src/application/interfaces/delivery_card_repository.py; src/application/services/delivery_orchestrator.py; src/config/container.py; scripts/run_runtime_cycle.py; tests/unit/infrastructure/test_runtime_infrastructure.py; docs/ARCHITECTURE_OVERVIEW.txt; docs/DECISIONS.txt; docs/TECH_DEBT.txt; docs/SETUP_WINDOWS.txt
Причина: Перевести проект из состояния «архитектурный каркас + policy-логика» в управляемый исполняемый runtime-контур без перехода к production-инфраструктуре.
Что сделано:
- Добавлен runtime execution слой: DeliveryRuntime + DeliveryRunSummary с синхронным process_cycle(seed + run_once).
- Добавлены in-memory infrastructure-компоненты: InMemoryDeliveryCardRepository и InMemoryDeliveryQueue.
- В application-слой добавлен контракт DeliveryCardRepository без переноса business-логики в инфраструктуру.
- Container расширен явным wiring runtime-контура (orchestrator + repository + queue + runtime service).
- Добавлен CLI entrypoint scripts/run_runtime_cycle.py для локального запуска одного цикла.
- Добавлены unit-тесты runtime/infrastructure и подтвержден зеленый прогон test contour.
Как теперь выполняется delivery cycle:
1) runtime вызывает provider ready результатов через orchestrator;
2) создаются DeliveryCard и сохраняются в in-memory repository;
3) id карточек помещаются в in-memory FIFO queue;
4) run_once последовательно обрабатывает карточки и сохраняет обновленное состояние;
5) runtime возвращает DeliveryRunSummary для operator-facing сводки.
Что осталось stub-уровнем:
- Renovatio/MAX/Email остаются stub-адаптерами integration-слоя.
- Персистентность, брокер, scheduler, real HTTP/API не подключались.
Ограничения текущего runtime:
- persistence и queue ограничены процессом и теряются после рестарта;
- отсутствует реальное отложенное планирование, backoff и фоновые worker-потоки;
- нет межпроцессной дедупликации.
Следующий шаг: Добавить persistent-реализацию репозитория, затем вынести queue/runner в управляемый process manager с сохранением текущих слоевых границ.

[Изменение]
Дата: 2026-04-13
Ветка: work
Блок: Точечная доработка runtime / infrastructure слоя
Файлы: src/infrastructure/runtime/delivery_runtime.py; src/infrastructure/repositories/in_memory_delivery_card_repository.py; src/infrastructure/identity/delivery_card_identity.py; src/infrastructure/identity/__init__.py; docs/TECH_DEBT.txt; docs/ARCHITECTURE_OVERVIEW.txt
Причина: Устранить найденные точечные дефекты runtime summary и дублирование вычисления card id без расширения функциональности за рамки текущего этапа.
Какие дефекты найдены:
- `created_cards_count` в `run_once` возвращался заглушкой `0`, что приводило к рассогласованию summary при pipeline `seed + run_once`.
- Формирование operational card id дублировалось в runtime и repository.
Что исправлено:
- `DeliveryRuntime.run_once` теперь принимает `created_cards_count` и возвращает его в summary, а `process_cycle` передает фактическое количество карточек, созданных на этапе seed.
- `process_cycle` возвращает результат `run_once` без ручной пересборки summary, чтобы исключить расхождения между методами.
- Добавлена единая инфраструктурная точка вычисления id (`build_operational_card_id`), которую используют и runtime, и repository.
Какие ограничения явно зафиксированы:
- Runtime по-прежнему выполняет только один управляемый синхронный цикл и не делает автоматический re-enqueue для карточек в `waiting_retry`.
- In-memory queue/repository остаются процесс-локальными: повторные process cycle между перезапусками не имеют устойчивого persistent-state и не гарантируют global deduplication.
Почему не внедряется re-enqueue/persistent dedup сейчас:
- Текущий этап ограничен точечным фиксом runtime/infrastructure без внедрения scheduler/background execution и без добавления persistent-хранилища.
- Расширение в сторону планировщика, брокеров и БД перенесено в следующий крупный инфраструктурный блок.


[Изменение]
Дата: 2026-04-13
Ветка: work
Блок: Реализация persistence / database слоя
Файлы: src/infrastructure/persistence/*; src/config/container.py; src/config/runtime_settings.py; tests/unit/infrastructure/persistence/*; alembic/*; alembic.ini; requirements.txt; docs/*
Причина: Обеспечить долговременное хранение карточек доставки и подготовить runtime к повторным process cycle.
Что сделано:
- Выбран и подключен стек SQLAlchemy + Alembic + PostgreSQL.
- Добавлены ORM-модели delivery_cards и delivery_attempts, а также явные mapper'ы domain <-> persistence.
- Реализован PostgresDeliveryCardRepository как альтернатива in-memory репозиторию по тому же контракту.
- Введен dual-mode runtime wiring через env-флаг SLD_REPOSITORY_MODE.
- Добавлена начальная миграция Alembic для таблиц delivery_cards/delivery_attempts.
- Расширен тестовый контур тестами mapping и repository операций сохранения/чтения/обновления.
Что изменилось в архитектуре: Infrastructure получила persistence contour, domain/application остались изолированными от ORM-деталей.
Ограничения: Полный integration-контур с реальным PostgreSQL в CI пока не внедрен.
Следующий шаг: Добавить CI-job с PostgreSQL service и тесты повторных process cycle в postgres-режиме runtime.

[Изменение]
Дата: 2026-04-13
Ветка: work
Блок: Реализация operator API (read-only)
Файлы: src/presentation/operator_api/app.py; src/presentation/operator_api/dependencies.py; src/presentation/operator_api/routers/cards.py; src/presentation/operator_api/schemas/delivery_cards.py; src/application/services/delivery_card_read_service.py; src/config/container.py; scripts/run_operator_api.py; tests/integration/test_operator_api_read_only.py; requirements.txt; docs/PROJECT_JOURNAL.txt; docs/ARCHITECTURE_OVERVIEW.txt; docs/DECISIONS.txt; docs/SETUP_WINDOWS.txt; docs/TEST_PLAN.txt
Причина: После стабилизации persistence добавить операторский слой наблюдаемости без write-side вмешательства в delivery flow.
Что сделано:
- Добавлен FastAPI presentation-слой для read-only operator API.
- Реализованы endpoint-ы GET /operator/cards, GET /operator/cards/{card_id}, GET /operator/cards/summary.
- В application добавлен отдельный DeliveryCardReadService с фильтрами (status, queue_status, channel, patient_id, lab_result_id, only_active, only_done).
- Введены явные response-схемы DeliveryAttemptResponse, DeliveryCardResponse, DeliveryCardSummaryResponse.
- AppContainer расширен read stack wiring (repository -> DeliveryCardReadService).
- Добавлен локальный запуск API через scripts/run_operator_api.py.
- Добавлены базовые integration-тесты operator API для in-memory и postgres repository (sqlite surrogate).
Что изменилось в логике: Бизнес-логика доставки, policy-решения и write-side use cases не изменялись.
Ограничения блока:
- API остается строго read-only; команды ручного вмешательства не добавлялись.
- Фильтрация реализована на текущем query-слое без пагинации и без расширенной сортировки.
- Auth/roles и operator UI не вводились на этом этапе.
Следующий шаг: выбрать следующий функциональный блок между operator command layer, scheduler/retry manager или real transport integrations.

[Багфикс]
Дата: 2026-04-13
Ветка: work
Блок: Точечный фикс operator API testing и локального запуска
Файлы: tests/integration/test_operator_api_read_only.py; scripts/run_operator_api.py; docs/SETUP_WINDOWS.txt; docs/PROJECT_JOURNAL.txt
Причина: Интеграционный тест operator API с SQLAlchemy/SQLite был нестабилен из-за разных connection для in-memory БД, а прямой запуск скрипта operator API из корня падал с ModuleNotFoundError по пакету src.
Что сделано:
- В integration-тесте test_operator_api_works_with_postgres_repository изменена конфигурация sqlite in-memory на единый connection pool через StaticPool и connect_args check_same_thread=False.
- Зафиксирован запуск scripts/run_operator_api.py напрямую из корня проекта через минимальный bootstrap sys.path до импорта src.
- Модульный запуск python -m scripts.run_operator_api сохранен без изменений.
Почему падал integration test:
- Таблицы создавались в одном connection, а repository/API могли открывать другой connection, из-за чего in-memory sqlite не видела таблицу delivery_cards.
Как исправлена конфигурация sqlite:
- Для теста используется общий in-memory engine c StaticPool, что гарантирует единое подключение и видимость схемы во всех сессиях теста.
Почему прямой запуск run_operator_api.py не работал:
- При запуске файла как скрипта Python формировал sys.path относительно каталога scripts, поэтому пакет src из корня проекта не резолвился.
Как это исправлено:
- Добавлен точечный bootstrap: при прямом запуске в sys.path добавляется корень проекта (родитель scripts), после чего импорт src.presentation.operator_api становится доступен.
Что изменилось в архитектуре:
- Архитектура API и границы слоев не менялись; исправлены только тестовая конфигурация и способ локального старта скрипта.

[Изменение]
Дата: 2026-04-14
Ветка: work
Блок: Реализация scheduler / process manager слоя
Файлы: src/infrastructure/runtime/delivery_runtime.py; src/infrastructure/runtime/delivery_runtime_selector.py; src/infrastructure/runtime/delivery_process_manager.py; src/infrastructure/runtime/__init__.py; src/config/container.py; scripts/run_process_manager.py; tests/unit/infrastructure/test_delivery_process_manager.py; docs/ARCHITECTURE_OVERVIEW.txt; docs/DECISIONS.txt; docs/TECH_DEBT.txt; docs/SETUP_WINDOWS.txt; docs/TEST_PLAN.txt; docs/PROJECT_JOURNAL.txt
Причина: После подключения persistence системе требовался управляемый execution lifecycle между циклами, включая повторную обработку карточек в waiting_retry.
Что сделано:
- Добавлен DeliveryRuntimeSelector как отдельный selection/query слой для runtime-кандидатов.
- Добавлен DeliveryProcessManager с lifecycle API: run_once, run_retry_cycle, run_all_pending.
- Введен ProcessManagerSummary с метриками цикла (new/reprocessed/retry/processed/outcome counters).
- DeliveryRuntime расширен методом enqueue_cards для повторной постановки существующих карточек в очередь.
- Container wiring расширен компонентами selector + process manager.
- Добавлен CLI-скрипт scripts/run_process_manager.py для прозрачного локального запуска lifecycle цикла.
Какие правила сохранены:
- Scheduler/process manager не принимает retry/fallback/deduplication решений.
- Business-решения остались в policy/use-case слое application.
- Persistence используется как источник состояния между циклами.
Что покрыто тестами:
- Подбор waiting_retry карточек.
- Исключение done/manual_review/exhausted из pending-переобработки.
- Проверка run_once-сценария process manager.
- Проверка summary-метрик и postgres-совместимости selector (sqlite surrogate).
Ограничения этапа:
- Реализация синхронная, без background scheduling.
- Нет distributed execution и broker-based orchestration.
Следующий шаг: После стабилизации process manager перейти к command-side operator сценариям и интеграции real transport с учетом безопасных ручных команд.

[Изменение]
Дата: 2026-04-14
Ветка: work
Блок: Реализация real transport integrations
Файлы: src/config/runtime_settings.py; src/config/integration_settings.py; src/config/container.py; src/integration/errors.py; src/integration/renovatio/client.py; src/integration/delivery/max_provider.py; src/integration/delivery/email_provider.py; src/integration/design/max_mini_app_contract.txt; tests/unit/integration/*; docs/PROJECT_JOURNAL.txt; docs/ARCHITECTURE_OVERVIEW.txt; docs/DECISIONS.txt; docs/TECH_DEBT.txt; docs/ENVIRONMENT_RULES.txt; docs/SETUP_WINDOWS.txt; docs/TEST_PLAN.txt
Причина: Перейти от stub-транспорта к рабочему real integration контуру без переноса бизнес-логики из policy/application в адаптеры.
Что сделано:
- Добавлены real-режимы для Renovatio/MAX/Email адаптеров при сохранении stub-режимов.
- Введен переключатель интеграций `SLD_INTEGRATION_MODE` и env-конфиг для каждого адаптера.
- Добавлен централизованный integration error mapping (`IntegrationFailure` + `IntegrationErrorKind`).
- Подготовлен контракт MAX mini app/deep link в design-слое.
- Добавлены unit-тесты на mapping/error handling/request building и dual-mode wiring контейнера.
Что изменилось в архитектуре:
- Появился явный dual-mode контур transport/integration без изменения границ domain/application/policy.
Какие ограничения обнаружены:
- patient_id -> MAX recipient_id пока задается конфигурацией (tech debt).
- Mini app ограничен контрактом deep-link без UI-реализации.
- Renovatio покрыт только методами, нужными текущему delivery flow.
Что остается незавершенным:
- production hardening (ретраи транспорта, мониторинг, rate-limit control, секрет-менеджмент).
- живые интеграционные тесты с внешними системами.

[Изменение]
Дата: 2026-04-14
Ветка: work
Блок: Очистка real integration adapters от stub-эвристик
Файлы: src/integration/delivery/max_provider.py; src/integration/delivery/email_provider.py; src/integration/renovatio/client.py; tests/unit/integration/test_max_real_adapter.py; tests/unit/integration/test_email_real_adapter.py; tests/unit/integration/test_stub_mode_adapters.py; docs/TECH_DEBT.txt; docs/PROJECT_JOURNAL.txt
Причина: Убрать тестовые эвристики из real-контура интеграций и зафиксировать разделение ответственности между real/stub режимами.
Что сделано:
- Удалены эвристики `endswith(...)` из адаптеров MAX и Email; deterministic stub-логика оставлена только в `mode="stub"` через явные наборы идентификаторов.
- В real-режиме MAX/Email адаптеры больше не симулируют ошибки и возвращают ошибку только при проблемах transport/config/mapping.
- Для MAX-доставки добавлен docstring-комментарий о временном identity mapping (`patient_id -> MAX user_id`) и необходимости выделения в отдельный service слой.
- В Renovatio adapter оставлен dual-mode с явным комментарием о временности совмещенной реализации (stub via `seed_results`, real via HTTP).
- Добавлены unit-тесты, отдельно проверяющие deterministic stub-поведение адаптеров, и обновлены real-тесты, чтобы они не опирались на суффиксы id.
Какие ограничения real integration зафиксированы:
- В real transport контуре не допускается искусственная симуляция ошибок внутри адаптера.
- Ошибки real-режима ограничены transport/config/внешним контрактом интеграции.
- Renovatio/MAX/Email все еще работают в dual-mode реализациях в рамках одного класса (tech debt).

[Изменение]
Дата: 2026-04-14
Ветка: work
Блок: Реализация operator command layer
Файлы: src/application/services/policies/operator_action_policy.py; src/application/use_cases/operator_commands.py; src/presentation/operator_api/routers/commands.py; src/presentation/operator_api/schemas/commands.py; src/presentation/operator_api/app.py; src/config/container.py; src/integration/logging/operator_action_logger_adapter.py; tests/unit/application/test_operator_commands.py; tests/integration/test_operator_api_read_only.py; docs/ARCHITECTURE_OVERVIEW.txt; docs/DECISIONS.txt; docs/TECH_DEBT.txt; docs/SETUP_WINDOWS.txt; docs/TEST_PLAN.txt; docs/PROJECT_JOURNAL.txt
Причина: Требовалось добавить управляемый command-side для оператора после стабилизации read-only API и scheduler/process manager.
Какие команды добавлены:
- RetryDeliveryCardCommandUseCase и endpoint POST /operator/cards/{card_id}/retry.
- MoveToManualReviewCommandUseCase и endpoint POST /operator/cards/{card_id}/manual-review.
- RequeueDeliveryCardCommandUseCase и endpoint POST /operator/cards/{card_id}/requeue.
- OverrideChannelCommandUseCase и endpoint POST /operator/cards/{card_id}/override-channel.
Какие действия разрешены:
- Retry для нетерминальных карточек вне manual_review/done.
- Перевод в manual_review для обрабатываемых карточек.
- Requeue только из manual_review/waiting_retry.
- Override канала только для нетерминальных карточек.
Какие ограничения введены:
- Нет произвольной команды "поставить любой статус".
- Все проверки допустимости централизованы в OperatorActionPolicy.
- Роутеры не пишут в repository напрямую, команды проходят через use case слой.
Связь с process manager:
- Operator commands изменяют состояние карточки и/или запускают существующий retry use case.
- Scheduler/process manager остается отдельным механизмом циклической обработки.
Что сознательно не поддерживается:
- RBAC/ACL enterprise-уровня.
- Произвольное редактирование карточек и универсальный PATCH статусов.
- Distributed locking.
Следующий шаг: усилить audit trail до persistence-уровня (таблица operator_action_log) и добавить role-aware авторизацию команд.

[Багфикс]
Дата: 2026-04-14
Ветка: work
Блок: Operator command layer refinement (retry/requeue/override ограничения)
Файлы: src/application/use_cases/operator_commands.py; src/application/services/policies/operator_action_policy.py; tests/unit/application/test_operator_commands.py; tests/integration/test_operator_api_read_only.py; docs/PROJECT_JOURNAL.txt; docs/TECH_DEBT.txt
Причина: Ужесточить operator command layer и убрать неоднозначную ручную мутацию карточек без изменения общей архитектуры.
Что сделано:
- Requeue зафиксирован как controlled preparation for next processing cycle, а не произвольный ручной lifecycle-edit.
- В command use case добавлен явный комментарий на русском о временном упрощении FAILED -> NOT_STARTED как технической подготовки к следующему циклу.
- Retry command явно документирован как thin-wrapper поверх existing RetryDeliveryUseCase без собственной retry-бизнес-логики.
- Override channel ужесточен policy-правилами: запрет same-channel override, запрет для terminal delivery статусов, запрет для закрытого queue_status.
- Формализация policy повышена через явные наборы terminal/closed состояний и детерминированные причины allow/deny.
- Добавлены проверки unit/integration на запрещенные сценарии и подтверждено, что endpoints возвращают контролируемые 4xx (409), а не 500.
Что изменилось в логике:
- Operator requeue теперь всегда приводит карточку к queue_status=active, а при статусе failed подготавливает status=not_started перед постановкой в следующий цикл.
- Operator override больше не допускает no-op override на текущий канал.
Ограничения и осознанные допущения:
- Полноценная модель operator workflow (расширенные state-machine сценарии, RBAC, конкуренция команд) остается отдельным этапом.
Следующий шаг: При переходе к production-hardening выделить сценарный слой operator workflow и усилить аудит/конкурентный контроль.

[Изменение]
Дата: 2026-04-14
Ветка: work
Блок: Реализация patient mini app / MAX UI layer
Файлы: src/application/services/patient_result_read_service.py; src/application/services/__init__.py; src/presentation/patient_api/*; src/presentation/operator_api/app.py; tests/integration/test_patient_api_read_only.py; frontend/miniapp/*; docs/PROJECT_JOURNAL.txt; docs/ARCHITECTURE_OVERVIEW.txt; docs/DECISIONS.txt; docs/TECH_DEBT.txt; docs/SETUP_WINDOWS.txt; docs/ENVIRONMENT_RULES.txt; docs/TEST_PLAN.txt
Причина: Добавить первый patient-facing контур внутри MAX с изоляцией UI, MAX context и patient read API.
Что сделано:
- Добавлен отдельный frontend-контур `frontend/miniapp` (React + TypeScript) с базовым flow список -> детали.
- Добавлен MAX context adapter (`window.WebApp`, `start_param`, platform/version) с fallback-режимом вне MAX.
- Добавлен frontend API client слой (`api/client.ts`, `api/results.ts`) с централизованной обработкой ошибок.
- Добавлен patient-facing read API (`/patient/results`, `/patient/results/{id}`) с отдельным контрактом относительно operator API.
- Добавлены frontend unit/API tests и backend integration tests для patient consumer.
Ограничения:
- max-ui пакет из приватного реестра недоступен в текущем окружении; используется совместимый shim-слой и зафиксирован tech debt.
- Документ результата (`/patient/results/{id}/document`) пока как readiness contract/stub без реальной выдачи файла.
Следующий шаг: реализовать серверную валидацию initData/start_param и включить реальный MAX UI package при доступе к приватному registry.

[Изменение]
Дата: 2026-04-14
Ветка: work
Блок: Stabilization frontend layer (mini app)
Файлы: frontend/miniapp/package.json; frontend/miniapp/vitest.config.ts; frontend/miniapp/tsconfig.json; frontend/miniapp/vite-env.d.ts; frontend/miniapp/src/App.test.tsx; frontend/miniapp/src/api/results.test.ts; docs/TECH_DEBT.txt; docs/PROJECT_JOURNAL.txt
Причина: Mini app frontend должен стабильно собираться и запускать тесты без изменения текущей архитектуры слоев.
Что сделано:
- Добавлена optional dependency `@maxhub/max-ui` в `package.json` с сохранением текущего shim-подхода через alias.
- Vitest конфигурация дополнена `globals: true`, окружение `jsdom` сохранено.
- TypeScript конфигурация обновлена (`types: ["vitest/globals", "node"]`, `lib: ["ESNext", "DOM"]`) и добавлен `vite-env.d.ts`.
- В тестах замена `global` на `globalThis` для корректной типизации и совместимости.
- Удален неиспользуемый `@ts-expect-error` в App тесте.
Что НЕ изменялось сознательно:
- Бизнес-логика domain/application/integration.
- Backend API, runtime/process manager и transport-адаптеры.
- Архитектурные границы проекта.
Проверки:
- `npm run build`.
- `npx vitest run`.
Риски/ограничения:
- Реальные MAX UI компоненты по-прежнему не подключены напрямую в runtime; используется shim до отдельного интеграционного шага.
Связанные блоки: patient-facing mini app, frontend test infra, technical debt registry.


=== 2026-04-14 — Production hardening / observability / security foundations ===

Что сделано:
- Добавлен server-side security foundation для patient/MAX контура: введена проверка `X-Max-Init-Data` по hash-контракту WebAppData, запрет безусловного доверия query `patient_id` в strict-режиме и сверка `start_param` с подписанным контекстом.
- Реализован audit trail operator commands: добавлена persistence-таблица `operator_action_log`, ORM-модель и repository; operator command use cases расширены audit-контекстом (`reason/actor/source/error`) и пишут устойчивый аудит через adapter.
- Введён structured logging bootstrap: единая JSON-like настройка логов, structured поля для integration failures/operator audit/process manager/api ошибок.
- Добавлены operational endpoints `/health/live` и `/health/ready`; readiness проверяет app/db/config/integration-mode без запуска бизнес-логики.
- Усилена config discipline: добавлен `SLD_ENV`, валидация режимов `repository/integration/security`, prod-like проверка обязательных интеграционных секретов.
- Добавлен базовый rate/misuse protection middleware для `/patient/results` и `/operator/cards`.
- Унифицирован API error contract (code/message/correlation_id/timestamp), исключены неконтролируемые traceback-ответы.

Ограничения:
- MAX initData верифицируется сервером при наличии `SLD_MAX_WEBAPP_SECRET`; в relaxed-режиме допускается fallback query patient_id для dev/test.
- Rate limiter реализован как in-memory middleware и не предназначен для горизонтального масштабирования.

[Изменение]
Дата: 2026-04-14
Ветка: work
Блок: Final launch readiness / go-live preparation
Файлы: src/config/runtime_settings.py; src/config/launch_mode_validation.py; src/presentation/common/health.py; scripts/run_launch_readiness_checks.py; scripts/run_patient_api.py; tests/unit/config/test_launch_mode_validation.py; tests/smoke/test_launch_readiness_smoke.py; docs/GO_LIVE_CHECKLIST.txt; docs/RUNBOOK.txt; docs/KNOWN_LIMITATIONS.txt; docs/ARCHITECTURE_OVERVIEW.txt; docs/DECISIONS.txt; docs/TECH_DEBT.txt; docs/ENVIRONMENT_RULES.txt; docs/SETUP_WINDOWS.txt; docs/TEST_PLAN.txt
Причина: Провести финальную системную подготовку к запуску без расширения бизнес-функциональности.
Что сделано:
- Добавлен формальный launch env-контракт с профилями dev/test/staging/prod и проверкой unsafe defaults.
- Добавлен единый smoke-скрипт `run_launch_readiness_checks.py` для миграций, health, runtime/process manager, API/audit и mini app build.
- Подготовлены обязательные go-live документы: checklist, runbook, known limitations.
- Усилен тестовый контур launch-ready unit/smoke проверками.
Что подтверждено перед запуском:
- Система может быть проверена единым launch smoke сценарием.
- Критичные release-like конфиги формально валидируются и блокируют небезопасный запуск.
- Есть явный recovery/rollback минимум и операционная инструкция.
Оставшиеся ограничения:
- Часть проверок остается manual (реальные внешние транспорты, mini app в целевой среде).
- Rate limit и часть операционных механизмов остаются in-memory уровнем.
Текущий статус готовности проекта: Ready for controlled go-live rehearsal / handoff с явным перечнем ограничений.

[Багфикс]
Дата: 2026-04-16
Ветка: work
Блок: Фиксация staging-исправлений (MAX + nginx + mini app)
Файлы: src/presentation/operator_api/app.py; src/presentation/patient_api/app.py; tests/integration/test_hardening_endpoints.py; frontend/miniapp/index.html; frontend/miniapp/src/max/context.ts; frontend/miniapp/src/max/context.test.ts; frontend/miniapp/src/App.tsx; frontend/miniapp/src/App.test.tsx; docs/ENVIRONMENT_RULES.txt; docs/SETUP_WINDOWS.txt; docs/TECH_DEBT.txt; docs/PROJECT_JOURNAL.txt
Причина: На staging исправления были сделаны вручную на VPS, но не зафиксированы в Git, что создавало риск расхождения окружений.
Что сделано:
- Для FastAPI приложений зафиксированы `root_path` значения `/api/operator` и `/api/patient` для корректной работы за nginx prefix.
- Добавлены интеграционные проверки Swagger/OpenAPI за prefix и доступности health endpoint-ов.
- В mini app подключен официальный MAX WebApp bridge (`https://st.max.ru/js/max-web-app.js`) до старта React.
- MAX context слой расширен: чтение `initData`, `initDataUnsafe`, `start_param`, `user.id`, `platform`, `version`, а также флаги `isInsideMax`, `hasInitData`, `hasUser`, `hasStartParam`.
- Убран localhost fallback для API: теперь base URL берется из `VITE_PATIENT_API_BASE_URL`, иначе используется relative `/api/patient`.
- Добавлена временная dev-only UI диагностика (`insideMax`, `hasInitData`, `patientId`) и unit-тесты context/base URL smoke.
- Обновлены ENV/SETUP/TECH_DEBT документы для воспроизводимости staging-поведения через Git.
Что изменилось в архитектуре:
- Префиксная reverse-proxy совместимость закреплена на уровне приложения и тестов.
- MAX bridge (transport/runtime) остается изолированным от API-клиента; логика base URL централизована в frontend слое конфигурации.
Что нужно проверить:
- Доступность `/api/operator/docs`, `/api/operator/openapi.json`, `/api/patient/docs`, `/api/patient/openapi.json` за nginx.
- Корректность mini app контекста как внутри MAX, так и вне MAX.
- Отсутствие localhost URL в сетевых запросах mini app.
Риски / ограничения:
- Dev-only диагностические поля в UI нужно удалить после стабилизации.
- Клиентский MAX context по-прежнему требует server-side валидации подписи для полного security-контура.
Связанные блоки: Staging hardening, Nginx reverse proxy, MAX mini app.

[Багфикс]
Дата: 2026-04-16
Ветка: codex/consolidate-staging-fixes-into-codebase
Блок: Исправление real HTTP-контракта Renovatio и добавление локальной диагностической проверки
Файлы: src/integration/renovatio/client.py; tests/unit/integration/test_renovatio_real_adapter.py; scripts/test_renovatio_real.py; src/config/integration_settings.py; docs/ENVIRONMENT_RULES.txt; docs/SETUP_WINDOWS.txt; docs/TECH_DEBT.txt; docs/PROJECT_JOURNAL.txt
Причина: Real-режим Renovatio использовал неверный HTTP-контракт (method/api_version в form body и POST на base_url без method path), из-за чего невозможно корректно проверить живой API.
Что было неверно в контракте:
- method передавался в body вместо URL path;
- вызов выполнялся на base_url без добавления имени метода;
- фактический контракт Renovatio требует POST на `<base_url>/<version>/<method>` или `<base_url>/<method>`.
Что сделано:
- В `RenovatioClient._call_api` method перенесен в URL path, а в form body оставлены только `api_key` и payload параметров метода.
- Добавлена сборка URL с учетом опционального `SLD_RENOVATIO_API_VERSION`.
- Добавлен локальный диагностический скрипт `python scripts/test_renovatio_real.py` для безопасной проверки `getPatient`, `getPatientLabResults`, `getReadyResults`.
- Обновлены unit-тесты real adapter на проверку URL/body-контракта и сохранения обработки ошибок.
- Уточнен дефолтный `SLD_RENOVATIO_BASE_URL` и документация env/локальной проверки.
Что изменилось в архитектуре:
- Dual-mode сохранен в существующем integration adapter (`stub`/`real`) без добавления новых слоев и дублирующих клиентов.
Ограничения:
- Это диагностический шаг и подготовка к live API, а не полноценный patient identification/auth flow.
- Скрипт не гарантирует, что patient_id=1 существует в конкретной инсталляции Renovatio.
Следующий шаг: реализовать реальный patient identification/auth flow поверх уже корректного real HTTP-контракта Renovatio.

[Изменение]
Дата: 2026-04-16
Ветка: work
Блок: Подготовка patient auth flow для Renovatio
Файлы: src/integration/renovatio/client.py; tests/unit/integration/test_renovatio_real_adapter.py; scripts/test_renovatio_patient_auth_flow.py; docs/PROJECT_JOURNAL.txt; docs/ENVIRONMENT_RULES.txt; docs/SETUP_WINDOWS.txt; docs/TECH_DEBT.txt
Причина: По результатам практической проверки контракта выяснено, что patient-facing методы Renovatio требуют patient_key (получаемый через authPatient/checkAuthCode), и этот foundation нужно заложить до интеграции в mini app и patient API.
Что сделано:
- В существующем Renovatio adapter явно разделены system-side методы и patient-facing методы без дублирования клиента.
- Исправлен системный вызов getPatient: теперь передается параметр id (вместо patient_id).
- Добавлены patient-facing методы: authPatient/checkAuthCode/refreshPatientKey/getPatientInfo/getPatientLabResults/getPatientLabResultDetails.
- Добавлен локальный диагностический entrypoint scripts/test_renovatio_patient_auth_flow.py для ручного прохождения auth + 2FA + проверок patient_key сценария.
- Обновлены unit-тесты real adapter на новый contract и payload-и.
Что изменилось в архитектуре:
- Dual-mode сохранен: runtime flow get_ready_results не изменялся, patient-facing методы доступны только в real режиме как контролируемое ограничение.
- Patient auth diagnostics вынесен в отдельный script и не встроен в production business flow.
Риски / ограничения:
- Foundation не реализует production session lifecycle и identity binding.
- Не реализована интеграция patient_key в backend runtime и mini app контур.
Следующий шаг:
- Встроить auth flow в backend/patient-facing контур с безопасным управлением lifecycle patient_key и последующим MAX↔patient linking.

2026-04-20 — Patient Auth Flow (Renovatio + Mini App)
- Добавлен серверный session-контур для patient-facing авторизации: login/password, phone + SMS code.
- Добавлены backend endpoints: /patient/auth/login, /phone, /confirm-code, /refresh, /logout, /me.
- В mini app добавлены экраны выбора способа входа, вход по логину, по телефону, подтверждение кода.
- Patient key полностью изолирован на backend, frontend работает только через cookie session.

[Багфикс]
Дата: 2026-04-21
Ветка: work
Блок: Fix: корректное создание patient session после authPatient
Файлы: src/application/use_cases/patient_auth.py; src/presentation/patient_api/routers/auth.py; tests/unit/application/test_patient_auth_use_cases.py; tests/integration/test_patient_auth_api.py; tests/integration/test_hardening_endpoints.py; docs/PROJECT_JOURNAL.txt; docs/TECH_DEBT.txt
Причина: После успешного `authPatient` backend ошибочно строил session напрямую из auth-ответа и возвращал 401 при отсутствии профиля/`patient_id`.
Что сделано:
- `authPatient` используется только как источник `patient_key`.
- Добавлен обязательный шаг `getPatientInfo(patient_key)` перед созданием session.
- Создание session переведено на профильные поля из `getPatientInfo`.
- Устранена ошибка 401 при успешном auth и `patient_id = null`.
- Введено семантическое разделение ошибок: auth/profile/session creation.
Что изменилось в логике: login и confirm-code flow теперь выполняют orchestration `auth -> profile -> session` в application-слое; router лишь маппит типы ошибок в controlled HTTP-ответы.
Что нужно проверить: `POST /patient/auth/login`, `POST /patient/auth/confirm-code`, `GET /patient/auth/me`, сценарий profile fetch failure, полный `pytest`.
Риски / ограничения: доступность `getPatientInfo` стала критичной частью login flow.

[Багфикс]
Дата: 2026-04-21
Ветка: work
Блок: Fix: URL-стратегия Renovatio patient методов + корректный profile mapping
Файлы: src/integration/renovatio/client.py; src/application/use_cases/patient_auth.py; tests/unit/integration/test_renovatio_real_adapter.py; tests/unit/application/test_patient_auth_use_cases.py; tests/integration/test_patient_auth_api.py; docs/PROJECT_JOURNAL.txt; docs/TECH_DEBT.txt
Причина: В текущей инсталляции Renovatio часть patient-facing методов не поддерживает versioned путь (`/api/public/{version}/{method}`) и возвращает `Method not found`; это ломало шаг `getPatientInfo` после успешного `authPatient` и приводило к 502 в login flow.
Что сделано:
- В Renovatio client внедрена allowlist-стратегия URL-роутинга: patient-facing методы вызываются только через unversioned путь `/api/public/{method}`.
- Для остальных методов сохранен текущий versioned маршрут `/api/public/{version}/{method}` (или unversioned при пустой версии).
- Улучшена диагностика API-ошибок: `code=404` + `Method not found` теперь явно маркируется как routing mismatch, а не маскируется как invalid credentials.
- Исправлен session profile mapping: `patient_number` берется из `number`; `patient_name` собирается из ФИО (`last_name`, `first_name`, `third_name`) с приоритетом `patient_name/full_name`.
- Добавлены/обновлены unit+integration тесты на URL-стратегию и profile mapping.
Что изменилось в архитектуре: URL-стратегия осталась в integration adapter, orchestration `auth -> profile -> session` осталась в application use case; роутеры не получили бизнес/HTTP-логики.
Что нужно проверить: `POST /patient/auth/login` => 200; `GET /patient/auth/me` возвращает корректные `patient_name` и `patient_number`; полный `pytest` зеленый.
Риски / ограничения: Поведение Renovatio может отличаться между инсталляциями; allowlist требует поддержки при добавлении новых patient-facing методов.

## 2026-04-21 — Renovatio patient results pipeline / shared backend for bot UI and mini app
Причина: `/patient/results*` был привязан к внутреннему delivery read-model (`patient_id`) и не использовал server-side session + `patient_key`.

Что сделано:
- Patient-facing endpoints `/patient/results` и `/patient/results/{id}` переведены на application use case `PatientResultsUseCase` с trust-source только из cookie-сессии (`sld_patient_session`).
- Старый `patient_id` query param сохранен только как формальная обратная совместимость и полностью игнорируется как источник доверия.
- Источник данных для patient-facing результатов теперь Renovatio методы `getPatientLabResults/getPatientLabResultDetails` по `patient_key`.
- Введены отдельные patient-facing контракты ответа: list item/details/documents без внутренних delivery-полей (`channel`, `attempts_count`, `last_error`).
- Для деталей подготовлен document/PDF contour: backend возвращает нормализованные документы (id/title/url/readiness/mime_type), без имитации несуществующих файлов.
- Добавлены unit/integration тесты для session-based сценариев, 401/404 и mapping payload.

Ограничения:
- Поддержка `lab_id`/`clinic_id` реализована как optional filter query params и прокидывается в Renovatio; финальные продуктовые значения и UX фильтров требуют дальнейшей калибровки.

[Багфикс]
Дата: 2026-04-21
Ветка: work
Блок: Test env isolation + process-manager service model + ZAPUSK228
Файлы: tests/conftest.py; tests/unit/scripts/test_zapusk228.py; scripts/zapusk228.py; scripts/__init__.py; deploy/systemd/smart-process-manager.service; deploy/systemd/smart-process-manager.timer; docs/PROJECT_JOURNAL.txt; docs/ARCHITECTURE_OVERVIEW.txt; docs/DECISIONS.txt; docs/TECH_DEBT.txt; docs/TEST_PLAN.txt; docs/SETUP_WINDOWS.txt; docs/RUNBOOK.txt; docs/GO_LIVE_CHECKLIST.txt
Причина:
- Backend тесты зависели от внешнего shell env (`SLD_INTEGRATION_MODE=real`, `SLD_REPOSITORY_MODE=postgres` и др.) и теряли stub/in-memory semantics.
- `smart-process-manager` запускался как one-shot, но service-модель с `Restart=always` приводила к бесконечным рестартам и `start-limit-hit`.
- Требовалась единая сопровождаемая operational точка запуска.
Что сделано:
- Добавлена глобальная test-изоляция критичных `SLD_*` переменных в `tests/conftest.py` (autouse fixture).
- Добавлены systemd артефакты `deploy/systemd/smart-process-manager.service` (oneshot, `Restart=no`) и `deploy/systemd/smart-process-manager.timer`.
- Реализован административный CLI `scripts/zapusk228.py` с интерактивными подтверждениями, dry-run и шагами update/deps/env/migrations/frontend/restart/smoke/tests.
- Добавлены unit-тесты ZAPUSK228 (`tests/unit/scripts/test_zapusk228.py`).
- Обновлены архитектурные/операционные документы, включая новое правило: при изменении operational flow обязательно оценивать обновление ZAPUSK228.
Что изменилось в логике:
- Production-логика delivery/patient контуров не менялась.
- Изменен только test/bootstrap и operational/runtime model для systemd.
Что нужно проверить:
- Целевые 4 регрессионных теста проходят даже при export production-like env.
- Полный `python -m pytest` зеленый.
- На сервере `smart-process-manager` работает через timer без restart-loop.

[Изменение]
Дата: 2026-04-21
Ветка: work
Блок: Patient Result PDF Download via session-based Renovatio flow
Файлы: src/application/use_cases/patient_results.py; src/config/container.py; src/presentation/patient_api/app.py; src/presentation/patient_api/dependencies.py; src/presentation/patient_api/routers/results.py; tests/unit/application/test_patient_results_use_case.py; tests/integration/test_patient_api_read_only.py; tests/integration/test_patient_auth_api.py; scripts/zapusk228.py; tests/unit/scripts/test_zapusk228.py; docs/PROJECT_JOURNAL.txt; docs/ARCHITECTURE_OVERVIEW.txt; docs/DECISIONS.txt; docs/TECH_DEBT.txt; docs/TEST_PLAN.txt; docs/SETUP_WINDOWS.txt; docs/RUNBOOK.txt; docs/GO_LIVE_CHECKLIST.txt
Причина: Завершить patient-facing data pipeline до уровня скачивания PDF по server-side session без утечки base64/patient_key на клиент.
Что сделано:
- Добавлен application use case `PatientResultPdfUseCase` с оркестрацией `session -> patient_key -> getPatientLabResultDetails -> data.files[0]`.
- Реализован endpoint `GET /patient/results/{result_id}/pdf` с бинарным `application/pdf` и `Content-Disposition`.
- Добавлена controlled обработка ошибок: 401 (нет session), 404 (result not found / PDF absent), 502 (invalid base64/payload).
- Сохранена совместимость существующего details endpoint `GET /patient/results/{id}`.
- Добавлено unit/integration покрытие для PDF сценариев и регрессии details/auth.
- Выполнена оценка operational contour: ZAPUSK228 расширен опциональным PDF smoke шагом через env (`SLD_SMOKE_PATIENT_SESSION`, `SLD_SMOKE_PATIENT_RESULT_ID`).
Какой контракт подтвержден:
- На реальном Renovatio-контуре PDF доступен в `getPatientLabResultDetails -> data.files[0]` как base64-строка.
- После decode получается валидный PDF (`%PDF` header), поэтому текущий flow не требует отдельного `file_id`.
Ограничения:
- Пока используется только первый файл из `data.files`; множественные документы требуют отдельного расширения модели.
Что нужно проверить:
- `python -m pytest` должен оставаться зеленым.
- Ручной smoke: login -> results -> details -> `/patient/results/{id}/pdf`.

[Изменение]
Дата: 2026-04-22
Ветка: work
Блок: Patient-facing mini app MVP (login -> results -> details -> open/download PDF) + foundation MAX binding
Файлы: src/application/use_cases/patient_results.py; src/presentation/patient_api/schemas/results.py; src/presentation/patient_api/routers/auth.py; src/application/use_cases/patient_auth.py; src/infrastructure/session/in_memory_external_patient_binding_repository.py; src/config/container.py; src/presentation/patient_api/app.py; frontend/miniapp/src/App.tsx; frontend/miniapp/src/components/ResultList.tsx; frontend/miniapp/src/components/ResultDetails.tsx; frontend/miniapp/src/api/results.ts; scripts/zapusk228.py; docs/*.txt; tests/*
Причина: Завершить первый пациентский MVP mini app поверх уже работающего backend-контра и зафиксировать архитектурный фундамент no-repeat-login через server-side binding.
Что сделано:
- Контракты `/patient/results*` нормализованы в patient-friendly DTO (title/status/has_pdf/lab_name/clinic_name + PDF actions route).
- Реализованы UX-сценарии mini app: login, home, results list, result details, open/download PDF.
- Добавлен серверный foundation external identity binding (`external_platform_user_id -> session_id`) без хранения credentials на клиенте.
- `/patient/auth/me` поддерживает восстановление сессии по привязке внешнего user_id (при отсутствии cookie).
- Добавлен `/patient/auth/unbind` для отвязки.
- ZAPUSK228 дополнен опциональным smoke login/results (по env credentials), PDF smoke сохранён.
Риски / ограничения:
- Нужна верификация финального стабильного MAX user-id поля (пока используется abstraction `external_platform_user_id`/header).
- Полноценный QR-flow не реализован, заложен как следующий архитектурный шаг.

[Багфикс]
Дата: 2026-04-22
Ветка: work
Блок: MINI APP POLISH + results consistency + PDF UX fix
Файлы: src/application/use_cases/patient_results.py; frontend/miniapp/src/App.tsx; frontend/miniapp/src/components/ResultList.tsx; frontend/miniapp/src/components/ResultDetails.tsx; frontend/miniapp/src/styles.css; tests/unit/application/test_patient_results_use_case.py; frontend/miniapp/src/App.test.tsx; docs/PROJECT_JOURNAL.txt; docs/DECISIONS.txt; docs/TEST_PLAN.txt
Причина: В patient-facing контуре фиксировались рассогласования list/details (result_id/status/has_pdf), нестабильный PDF UX в webview и «технический» вид mini app.
Что было причиной проблем:
- details мог вернуть пустой `id`, что приводило к невалидным PDF route;
- fallback статуса зависел от наличия PDF и расходился между list/details payload;
- open PDF через `window.open(url)` работал нестабильно по session/cookie в webview-сценариях;
- UI показывал слишком «сырой» MVP без нормальной карточной структуры и friendly-ошибок.
Что исправлено:
- В application-слое добавлена единая нормализация `result_id/status/has_pdf`.
- Для details введен обязательный fallback `result_id <- requested_result_id` и запрет построения PDF URL из пустого id.
- `has_pdf` унифицирован по признакам `has_pdf|is_pdf_available|files|files_count|documents`.
- В mini app open/download PDF переведены на единый fetch с `credentials: include` + Blob URL.
- Пользовательские ошибки PDF нормализованы в friendly-тексты без сырого JSON.
- UI списка/details переведен на аккуратные карточки с отступами и читаемой структурой.
Что нужно проверить:
- `python -m pytest tests/unit/application/test_patient_results_use_case.py`
- `python -m pytest tests/integration/test_patient_api_read_only.py tests/integration/test_patient_auth_api.py`
- `cd frontend/miniapp && npm run test -- --run`
- `cd frontend/miniapp && npm run build`
Ограничения и ZAPUSK228:
- Подтвержденных новых ограничений MAX webview не выявлено в этом блоке.
- Operational flow не менялся, поэтому обновление ZAPUSK228 не потребовалось.

[Изменение]
Дата: 2026-04-22
Ветка: work
Блок: FINAL RESULTS UX + clean mini app layout + placeholder visual system
Файлы: src/application/use_cases/patient_results.py; tests/unit/application/test_patient_results_use_case.py; frontend/miniapp/src/App.tsx; frontend/miniapp/src/components/ResultList.tsx; frontend/miniapp/src/components/ResultDetails.tsx; frontend/miniapp/src/styles.css; frontend/miniapp/src/ui/contentConfig.ts; frontend/miniapp/src/utils/patientResults.ts; frontend/miniapp/src/utils/patientResults.test.ts; frontend/miniapp/src/App.test.tsx; docs/PROJECT_JOURNAL.txt; docs/DECISIONS.txt; docs/TEST_PLAN.txt
Причина: Довести patient-facing results UX до clean medical MVP без изменения backend contracts и без тяжелых графических ассетов.
Что сделано:
- Список результатов в mini app упрощен до patient-facing минимума: `Результат №<id>`, дата, (опционально) лаборатория, статус.
- Details экран показывает номер результата, дату, лабораторию, статус, реальные показатели и рабочие PDF-кнопки.
- Реализован канонический mapping `services + results` по коду до символа `•` с fallback на `parameter_name`.
- Формат строки показателя закреплен как `Название: значение единица`.
- Добавлен легкий content-layer `frontend/miniapp/src/ui/contentConfig.ts` для заголовков, подписей кнопок и placeholder-блоков.
- Визуальный слой mini app переведен на clean карточный layout с placeholder visual system без фото/тяжелых баннеров.
- На backend усилена честная status-нормализация: при наличии PDF/реальных показателей статус отдается как `Готов`.
- Добавлены/обновлены unit tests backend и frontend на mapping/formatting/status/layout регрессии.
Что изменилось в архитектуре:
- Контрактная граница сохранена: backend отвечает за нормализацию и статус, frontend — за конфигурируемый рендер и UI-состояние.
- Mapping логика вынесена из JSX в отдельный util-слой mini app.
Что нужно проверить:
- `python -m pytest tests/unit/application/test_patient_results_use_case.py`
- `cd frontend/miniapp && npm run test -- --run`
- `cd frontend/miniapp && npm run build`
- ручной smoke login -> results -> details -> PDF в webview.
Ограничения и ZAPUSK228:
- Operational flow не изменялся, поэтому обновление `scripts/zapusk228.py` не потребовалось.

[Багфикс]
Дата: 2026-04-22
Ветка: work
Блок: MINI APP UX polish + patient values mapping + MAX PDF runtime flow
Файлы: frontend/miniapp/src/App.tsx; frontend/miniapp/src/components/ResultList.tsx; frontend/miniapp/src/utils/patientResults.ts; frontend/miniapp/src/styles.css; frontend/miniapp/src/App.test.tsx; frontend/miniapp/src/utils/patientResults.test.ts; src/presentation/patient_api/routers/results.py; docs/PROJECT_JOURNAL.txt; docs/DECISIONS.txt; docs/TEST_PLAN.txt
Причина: После стабилизации backend flow login->results->details->pdf в mini app оставались UX/runtime дефекты: второстепенная home-навигация, неполный рендер значений показателей, и нестабильное PDF open/download в MAX webview.
Что сделано:
- Главный экран переведен на крупный CTA-блок «Результаты анализов», старые мелкие кнопки убраны из primary-навигации.
- Приветствие нормализовано до human-friendly формата «Здравствуйте, Имя Отчество!» с fallback без awkward-пустых частей.
- Карточка результата сделана полностью кликабельной (open details), при этом сохранены явные action-кнопки (глаз/PDF/скачать) c stopPropagation.
- Исправлен mapping patient indicators: value/unit теперь берутся из canonical полей `parameter_value` и `measurement_unit` с fallback на старые ключи.
- PDF flow в mini app переведен на session-aware navigation URL (вместо blob-open): перед навигацией выполняется controlled availability-check, затем open/download идут через backend endpoint с query `disposition=inline|attachment`.
- Ошибки PDF в UI оставлены только в user-friendly форме без вывода raw JSON payload.
Что изменилось в архитектуре: backend-контракт patient results не ломался; добавлен только безопасный query-переключатель Content-Disposition на существующем PDF endpoint.
Что нужно проверить: vitest (App + patientResults), pytest regression для patient API PDF endpoint, и frontend build в mini app контуре.
Риски / ограничения: availability-check перед навигацией делает дополнительный сетевой запрос к PDF endpoint; это осознанный компромисс ради controlled ошибок и стабильности MAX runtime.
Связанные блоки: patient-facing mini app contour; MAX/webview runtime интеграция; patient results/details/pdf flow.

[Изменение]
Дата: 2026-04-22
Ветка: work
Блок: PDF VIEWER UX + BACK/SHARE/SAVE + MAX SEND FOUNDATION
Файлы: frontend/miniapp/src/App.tsx; frontend/miniapp/src/ui/contentConfig.ts; frontend/miniapp/src/styles.css; frontend/miniapp/src/App.test.tsx; docs/PROJECT_JOURNAL.txt; docs/DECISIONS.txt; docs/TEST_PLAN.txt; docs/TECH_DEBT.txt
Причина: Устранить UX-проблему “выпадения” из mini app при открытии PDF и добавить управляемые document-actions в patient-facing контуре.
Что сделано:
- Добавлен внутренний `pdf` screen в mini app (iframe + top bar + bottom action bar) вместо прямого raw-перехода на PDF URL.
- Реализован back-навигационный сценарий из PDF viewer с возвратом в source screen (`details` или `results`).
- В top bar добавлен logout с вызовом существующего server-side `/patient/auth/logout` и возвратом на login screen.
- Добавлены document actions: `Сохранить`, `Поделиться`, `Отправить в MAX`.
- Save flow реализован через fetch blob + download fallback; Share flow через Web Share API и clipboard fallback.
- Для `Отправить в MAX` заложен foundation: копирование ссылки + optional open bot link; неподтвержденный file-attach capability честно не заявляется как готовый.
- Добавлены frontend regression tests на viewer routing/top bar/logout/action bar/share fallback.
Что не менялось сознательно:
- Backend session-based PDF endpoint и auth/results/details контуры не переписывались.
- Operator/internal/Renovatio/operational контуры не затрагивались.
Ограничения:
- Прямой runtime API для attach PDF в чат MAX не подтвержден, зафиксировано как технический долг.
ZAPUSK228:
- Обновление не потребовалось: текущий operational flow уже покрывает frontend build и patient auth/results/pdf smoke hooks.

[Изменение]
Дата: 2026-04-24
Ветка: work
Блок: Full mini app UI redesign + managed navigation + results/pdf UX + foundation screens
Файлы: frontend/miniapp/src/App.tsx; frontend/miniapp/src/styles.css; frontend/miniapp/src/ui/contentConfig.ts; frontend/miniapp/src/App.test.tsx; docs/PROJECT_JOURNAL.txt; docs/DECISIONS.txt; docs/TEST_PLAN.txt; docs/TECH_DEBT.txt
Причина: Привести patient mini app к mobile-first UX медицинского приложения без изменения backend контрактов auth/session/results/details/pdf.
Что сделано:
- Переработан LoginScreen: крупные поля, плейсхолдеры, переключение видимости пароля, tel-ссылка клиники и human-readable ошибки.
- Введена управляемая навигация: нижние разделы Главная/Анализы/Запись/Акции/Услуги, а также back-навигация на вложенных экранах details/pdf.
- Пересобраны экраны Home/Results/ResultDetails/PDF, добавлены foundation экраны Appointment/Loyalty/Services.
- Services получили live search + дерево категорий/услуг из config.
- PDF viewer оставлен внутри mini app с fixed action bar (MAX/share/save), без прямого «потерянного» перехода.
- Добавлен/расширен content config слой для UI-текстов, labels и foundation/mock данных.
- Обновлены frontend тесты под новые UX-контракты.
Что изменилось в логике: backend API и session trust boundary не менялись; изменения только во frontend presentation/config/test слое.
Что изменилось в архитектуре: mini app UI перешел на mobile-first screen routing с явным разделением real-data (profile/results/details/pdf) и foundation-data (appointment/loyalty/services).
Что нужно проверить: `npm run test` и `npm run build` в `frontend/miniapp`, ручной smoke login->home->results->details->pdf, навигацию bottom/back.
Риски / ограничения:
- Appointment/Loyalty/Services работают как foundation/mock до подтверждения backend API.
- Отправка PDF в MAX остается fallback-сценарием через copy-link (без подтвержденного file-send API).
- ZAPUSK228 не менялся, так как operational/admin flow, backend endpoints и deploy pipeline не затронуты.
Связанные блоки: Patient auth/session, Patient results/details/pdf, Renovatio integration boundary, mini app MVP.

[Изменение]
Дата: 2026-05-01
Ветка: work
Блок: MINI APP UI ALIGNMENT + ASSET SYSTEM + PDF SHARE FILE FIX + API DISCOVERY PREP
Файлы: frontend/miniapp/src/App.tsx; frontend/miniapp/src/styles.css; frontend/miniapp/public/assets/*; docs/PROJECT_JOURNAL.txt; docs/DECISIONS.txt; docs/TECH_DEBT.txt; docs/TEST_PLAN.txt
Причина: Выравнивание patient mini app UI под референс и подготовка управляемой asset замены без backend-regression.
Что сделано: Обновлены login/home/results/details/pdf UI, добавлены asset placeholders, PDF share переведен на file-share сценарий с fallback-сообщением, зафиксированы foundation границы и discovery-требования.
Что не менялось: backend auth/session/results/details/pdf контракты и Renovatio integration contour.

[Изменение]
Дата: 2026-05-04
Ветка: work
Блок: Safe frontend UI alignment mini app под Figma reference
Файлы: frontend/miniapp/src/App.tsx; frontend/miniapp/src/styles.css; frontend/miniapp/src/ui/contentConfig.ts; frontend/miniapp/src/App.test.tsx; docs/DECISIONS.txt; docs/TEST_PLAN.txt; docs/TECH_DEBT.txt; docs/RUNBOOK.txt; docs/PROJECT_JOURNAL.txt
Причина: Подтянуть production-визуал mini app без изменения backend/API/session контуров.
Что сделано: Добавлены UI-токены и visual refinements, раскрывающаяся patient card, иконки карточек разделов, защита отображения даты рождения (fallback "не указана"), сохранен безопасный PDF share через file API, расширены frontend-тесты на collapse/expand profile card.
Что изменилось в логике: Контракты backend и auth/session flow не изменялись; изменения только presentation-слоя.
Что нужно проверить: npm test и npm run build в frontend/miniapp; smoke экраны login/home/results/details/pdf/services.
Ограничения: Appointment/Loyalty/Services остаются foundation-экранами без real backend integration.

[Изменение]
Дата: 2026-05-04
Ветка: work
Блок: Hybrid Figma Login integration (Phase 1)
Файлы: frontend/miniapp/src/App.tsx; frontend/miniapp/src/styles.css; docs/PROJECT_JOURNAL.txt; docs/DECISIONS.txt; docs/TEST_PLAN.txt
Причина: Обновить LoginScreen под Figma visual reference без изменения production auth/session flow.
Что сделано: Перенесены visual-слои login (карточка, отступы, типографика, блок помощи, иконка toggle пароля) с сохранением текущих auth API, session и runtime маршрутизации.
Что изменилось в логике: Бизнес-логика и API-контракты не изменялись; используется тот же handleLoginSubmit/authApi.login.
Риски / ограничения: Это только Phase 1 для LoginScreen; остальные экраны остаются на текущем UI.

- 2026-05-04: Hybrid Figma-like UI pass extended to Home/Results/Details/PDF/Appointments/Loyalty/Services with production routing/API/session preserved in miniapp App.tsx.

[Изменение]
Дата: 2026-05-06
Ветка: work
Блок: Стартовый MAX patient assistant runtime
Файлы: bot/package.json; bot/tsconfig.json; bot/src/index.ts; docs/DECISIONS.txt
Причина: Нужен отдельный кнопочный слой MAX-бота, не ломающий mini app и backend контур.
Что сделано:
- Добавлен отдельный TypeScript runtime в `bot/`.
- Подключен `MAX_BOT_TOKEN` из ENV (без hardcode).
- Реализовано стартовое меню `/start` и `bot_started` с кнопками: открыть mini app, профиль, анализы, последний результат, помощь.
- Добавлены базовые callback handler'ы с patient-friendly русскими ответами.
Ограничения:
- Пока не подключены backend endpoints профиля/auto-login/results/pdf.
- Нет state-machine и защищенного хранения профиля на backend в этом шаге.
Следующий шаг: подключить backend use cases и персистентный профиль/token слой, затем расширить bot UX по полному мастер-промту.

[Изменение]
Дата: 2026-05-06
Ветка: work
Блок: MAX BOT patient profile + mini app auto-login token (phase 2)
Файлы: src/application/use_cases/bot_patient.py; src/presentation/patient_api/routers/bot_profile.py; src/presentation/patient_api/routers/auth.py; src/presentation/patient_api/schemas/bot_profile.py; src/presentation/patient_api/app.py; src/config/container.py; frontend/miniapp/src/api/auth.ts; frontend/miniapp/src/App.tsx; tests/unit/application/test_bot_patient_use_cases.py; docs/*
Причина: Продолжение внедрения bot layer после мерджа стартового scaffold.
Что сделано:
- Добавлены backend use cases профиля бота, шифрования пароля и one-time mini app token.
- Добавлены endpoint'ы `/bot/profile/*`, `/bot/miniapp-token/create`, `/patient/auth/auto-login-token`.
- Добавлен mini app flow чтения `auto_login_token` из query и его redeem через backend.
- Добавлены unit tests для шифрования и one-time token behavior.
Ограничения:
- Профили/токены/состояние пока in-memory (без postgres migration в этом шаге).
- Bot runtime callbacks пока не подключены к новым backend endpoint'ам.

[Багфикс]
Дата: 2026-05-07
Ветка: work
Блок: Исправление MAX bot runtime сборки и SDK контрактов
Файлы: bot/package.json; bot/tsconfig.json; bot/src/index.ts; docs/PROJECT_JOURNAL.txt
Причина: Бот не собирался на сервере из-за невалидной версии `@maxhub/max-bot-api` и несовместимого использования SDK (`new Bot({ token })`, `ctx.answer`).
Что сделано:
- Обновлен bot package dependency на рабочий semver диапазон `@maxhub/max-bot-api:^0.2.2`.
- Переведен runtime на стиль SDK: `new Bot(token)` и ответы через `ctx.reply(...)`.
- Актуализированы callback handlers для кнопок меню и добавлен fallback чтения токена из `BOT_TOKEN` приоритетно после `MAX_BOT_TOKEN`.
- Зафиксировано разделение SDK: `@maxhub/max-ui` используется mini app UI, `@maxhub/max-bot-api` используется runtime обычного MAX чат-бота.
Ограничения:
- Проверка `npm install` может зависеть от доступности private/npm-registry для `@maxhub/max-bot-api` в серверном окружении.
